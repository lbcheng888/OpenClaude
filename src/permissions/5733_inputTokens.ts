// @ts-nocheck
import {b} from "../../runtime.ts";
import {Qr} from "../../vendor/m323.ts";
import {EZn,tVl} from "../../vendor/m5288.ts";
import {ve} from "../../vendor/m461.ts";
import {C} from "../../vendor/m321.ts";
var krr,ozm,Y9E,szm,J9E,izm,X9E,Q9E,azm,lzm,czm,Z9E,P$o,Hrr,uzm,kgc,dzm,pzm,mzm,Irr,fzm,hzm,O$o,e3E,Xyt,x$o,gzm,D$o,sjt,wgc,t3E,XSe,_zm,Hgc,IT,yzm,Tzm,Szm,bzm,Ezm,Czm,Azm,Rzm,vzm,wzm,kzm,Hzm,Igc,xgc,Izm,xzm,Dzm,Pzm,Ozm,Lzm,Mzm,Nzm,Fzm,Bzm,Uzm,$zm,qzm,Wzm,Gzm,Vzm,Kzm,zzm,jzm,Yzm,Jzm,Xzm,Qzm,Zzm,Dgc,ejm,tjm,njm,rjm,ojm,sjm,ijm,ajm,ljm,cjm,ujm,djm,pjm,mjm,fjm,hjm,gjm,_jm,yjm,Tjm,Sjm,bjm,n3E,ijt,L$o,M$o,Pgc,Ejm,Ogc,r3E,o3E,s3E,Cjm,Ajm,Rjm,ya,Lgc,N$o,vjm,F$o,Mgc,B$o,wjm,Ngc,kjm,Hjm,Ijm,Fgc,xjm,Bgc,Djm,Pjm,Ojm,Ugc,Ljm,Mjm,Njm,Fjm,U$o,$$o,Bjm,i3E,a3E,l3E,c3E,u3E,d3E,q$o,Ujm,$jm,qjm,p3E,m3E,f3E,h3E,g3E,_3E,Wjm,Gjm,Vjm,Kjm,zjm,jjm,Yjm,Jjm,Xjm,Qjm,Zjm,eYm,tYm,nYm,rYm,oYm,sYm,iYm,aYm,lYm,cYm,uYm,y3E,T3E,S3E,b3E,E3E,C3E,A3E,R3E,v3E,w3E,k3E,H3E,I3E,x3E,D3E,P3E,O3E,L3E,W$o,ajt;
var $gc=b(()=>{Qr();EZn();krr=ve(()=>C.object({inputTokens:C.number(),outputTokens:C.number(),cacheReadInputTokens:C.number(),cacheCreationInputTokens:C.number(),webSearchRequests:C.number(),costUSD:C.number(),contextWindow:C.number(),maxOutputTokens:C.number()})),ozm=ve(()=>C.literal("json_schema")),Y9E=ve(()=>C.object({type:ozm()})),szm=ve(()=>C.object({type:C.literal("json_schema"),schema:C.record(C.string(),C.unknown())})),J9E=ve(()=>szm()),izm=ve(()=>C.enum(["user","project","org","temporary","oauth"])),X9E=ve(()=>C.enum(["local","user","project"]).describe("Config scope for settings.")),Q9E=ve(()=>C.literal("context-1m-2025-08-07")),azm=ve(()=>C.object({type:C.literal("adaptive"),display:C.enum(["summarized","omitted"]).optional()}).describe("Claude decides when and how much to think (Opus 4.6+).")),lzm=ve(()=>C.object({type:C.literal("enabled"),budgetTokens:C.number().optional(),display:C.enum(["summarized","omitted"]).optional()}).describe("Fixed thinking token budget (older models)")),czm=ve(()=>C.object({type:C.literal("disabled")}).describe("No extended thinking")),Z9E=ve(()=>C.union([azm(),lzm(),czm()]).describe("Controls Claude's thinking/reasoning behavior. When set, takes precedence over the deprecated maxThinkingTokens.")),P$o=ve(()=>C.literal("comms").optional().catch(void 0).describe("@internal Coordinator-mode role for this MCP server. 'comms' marks the server the coordinator uses to address the user; the coordinator tool filter lets comms-roled servers' tools through. Claude Code extension to .mcp.json \u2014 host-side config, not part of the MCP wire protocol. Coordinator mode is activated via the CLAUDE_CODE_COORDINATOR_MODE environment variable; this field only takes effect when coordinator mode is active.")),Hrr=ve(()=>C.number().int().positive().optional().describe("Per-server tool-call timeout in milliseconds. Overrides the MCP_TOOL_TIMEOUT environment variable for this server. Hard wall-clock limit per call; progress notifications do not extend it. Values below 1000ms are ignored (falls through to MCP_TOOL_TIMEOUT or the default).")),uzm=ve(()=>C.object({type:C.literal("stdio").optional(),command:C.string(),args:C.array(C.string()).optional(),env:C.record(C.string(),C.string()).optional(),timeout:Hrr(),alwaysLoad:C.boolean().optional().describe("When true, all tools from this server are always included in the prompt and never deferred behind tool search. Equivalent to setting defer_loading: false on the API. Default: tools are deferred when tool search is enabled. As a side effect this also blocks startup until the server is connected (capped at the standard 5s connect timeout) even though MCP startup is otherwise non-blocking by default, since the tools must be present when the turn-1 prompt is built."),role:P$o()})),kgc=ve(()=>C.object({name:C.string(),permission_policy:C.enum(["always_allow","always_ask","always_deny"]).optional(),org_max_permission:C.enum(["allow","ask","blocked"]).optional().describe("Org admin's per-tool ceiling. Drives the auto-mode isOrgAskCeiling gate so an admin 'ask' cap forces a user prompt even in auto mode.")}).describe("Per-tool permission policy carried on mcp_set_servers for remote servers.")),dzm=ve(()=>C.object({type:C.literal("sse"),url:C.string(),headers:C.record(C.string(),C.string()).optional(),tools:C.array(kgc()).optional(),timeout:Hrr(),alwaysLoad:C.boolean().optional().describe("When true, all tools from this server are always included in the prompt and never deferred behind tool search. Equivalent to setting defer_loading: false on the API. Default: tools are deferred when tool search is enabled. As a side effect this also blocks startup until the server is connected (capped at the standard 5s connect timeout) even though MCP startup is otherwise non-blocking by default, since the tools must be present when the turn-1 prompt is built."),role:P$o()})),pzm=ve(()=>C.object({type:C.literal("http"),url:C.string(),headers:C.record(C.string(),C.string()).optional(),tools:C.array(kgc()).optional(),timeout:Hrr(),alwaysLoad:C.boolean().optional().describe("When true, all tools from this server are always included in the prompt and never deferred behind tool search. Equivalent to setting defer_loading: false on the API. Default: tools are deferred when tool search is enabled. As a side effect this also blocks startup until the server is connected (capped at the standard 5s connect timeout) even though MCP startup is otherwise non-blocking by default, since the tools must be present when the turn-1 prompt is built."),role:P$o()})),mzm=ve(()=>C.object({type:C.literal("sdk"),name:C.string()})),Irr=ve(()=>C.union([uzm(),dzm(),pzm(),mzm()])),fzm=ve(()=>C.object({type:C.literal("claudeai-proxy"),url:C.string(),id:C.string(),timeout:Hrr()})),hzm=ve(()=>C.union([Irr(),fzm()])),O$o=ve(()=>C.object({name:C.string().describe("Server name as configured"),status:C.enum(["connected","failed","needs-auth","pending","disabled"]).describe("Current connection status"),serverInfo:C.object({name:C.string(),version:C.string()}).optional().describe("Server information (available when connected)"),error:C.string().optional().describe("Error message (available when status is 'failed')"),config:hzm().optional().describe("Server configuration (includes URL for HTTP/SSE servers)"),scope:C.string().optional().describe("Configuration scope (e.g., project, user, local, claudeai, managed)"),tools:C.array(C.object({name:C.string(),description:C.string().optional(),annotations:C.object({readOnly:C.boolean().optional(),destructive:C.boolean().optional(),openWorld:C.boolean().optional()}).optional()})).optional().describe("Tools provided by this server (available when connected)"),capabilities:C.object({experimental:C.record(C.string(),C.unknown()).optional()}).optional().describe("@internal Server capabilities (available when connected). experimental['claude/channel'] is only present if the server's plugin is on the approved channels allowlist \u2014 use its presence to decide whether to show an Enable-channel prompt.")}).describe("Status information for an MCP server connection.")),e3E=ve(()=>C.object({added:C.array(C.string()).describe("Names of servers that were added"),removed:C.array(C.string()).describe("Names of servers that were removed"),errors:C.record(C.string(),C.string()).describe("Map of server names to error messages for servers that failed to connect")}).describe("Result of a setMcpServers operation.")),Xyt=ve(()=>C.enum(["userSettings","projectSettings","localSettings","session","cliArg"])),x$o=ve(()=>C.enum(["allow","deny","ask"])),gzm=ve(()=>C.enum(["allow","deny","ask","defer"])),D$o=ve(()=>C.object({toolName:C.string(),ruleContent:C.string().optional()})),sjt=ve(()=>C.discriminatedUnion("type",[C.object({type:C.literal("addRules"),rules:C.array(D$o()),behavior:x$o(),destination:Xyt()}),C.object({type:C.literal("replaceRules"),rules:C.array(D$o()),behavior:x$o(),destination:Xyt()}),C.object({type:C.literal("removeRules"),rules:C.array(D$o()),behavior:x$o(),destination:Xyt()}),C.object({type:C.literal("setMode"),mode:C.lazy(()=>XSe()),destination:Xyt()}),C.object({type:C.literal("addDirectories"),directories:C.array(C.string()),destination:Xyt()}),C.object({type:C.literal("removeDirectories"),directories:C.array(C.string()),destination:Xyt()})])),wgc=ve(()=>C.enum(["user_temporary","user_permanent","user_reject"]).describe("Classification of this permission decision for telemetry. SDK hosts that prompt users (desktop apps, IDEs) should set this to reflect what actually happened: user_temporary for allow-once, user_permanent for always-allow (both the click and later cache hits), user_reject for deny. If unset, the CLI infers conservatively (temporary for allow, reject for deny). The vocabulary matches tool_decision OTel events (monitoring-usage docs).")),t3E=ve(()=>C.union([C.object({behavior:C.literal("allow"),updatedInput:C.record(C.string(),C.unknown()).optional(),updatedPermissions:C.array(sjt()).optional(),toolUseID:C.string().optional(),decisionClassification:wgc().optional()}),C.object({behavior:C.literal("deny"),message:C.string(),interrupt:C.boolean().optional(),toolUseID:C.string().optional(),decisionClassification:wgc().optional()})])),XSe=ve(()=>C.enum(["default","acceptEdits","bypassPermissions","plan","dontAsk","auto"]).describe("Permission mode for controlling how tool executions are handled. 'default' - Standard behavior, prompts for dangerous operations. 'acceptEdits' - Auto-accept file edit operations. 'bypassPermissions' - Bypass all permission checks (requires allowDangerouslySkipPermissions). 'plan' - Planning mode, no actual tool execution. 'dontAsk' - Don't prompt for permissions, deny if not pre-approved. 'auto' - Use a model classifier to approve/deny permission prompts.")),_zm=["PreToolUse","PostToolUse","PostToolUseFailure","PostToolBatch","Notification","UserPromptSubmit","UserPromptExpansion","SessionStart","SessionEnd","Stop","StopFailure","SubagentStart","SubagentStop","PreCompact","PostCompact","PermissionRequest","PermissionDenied","Setup","TeammateIdle","TaskCreated","TaskCompleted","Elicitation","ElicitationResult","ConfigChange","WorktreeCreate","WorktreeRemove","InstructionsLoaded","CwdChanged","FileChanged","MessageDisplay"],Hgc=ve(()=>C.enum(_zm)),IT=ve(()=>C.object({session_id:C.string(),transcript_path:C.string(),cwd:C.string(),permission_mode:C.string().optional(),agent_id:C.string().optional().describe("Subagent identifier. Present only when the hook fires from within a subagent (e.g., a tool called by an AgentTool worker). Absent for the main thread, even in --agent sessions. Use this field (not agent_type) to distinguish subagent calls from main-thread calls."),agent_type:C.string().optional().describe('Agent type name (e.g., "general-purpose", "code-reviewer"). Present when the hook fires from within a subagent (alongside agent_id), or on the main thread of a session started with --agent (without agent_id).'),effort:C.object({level:C.string().describe('Active effort level for the current turn (e.g., "low", "medium", "high", "xhigh", "max"), after any silent downgrade for the selected model. Also exposed to hook commands and Bash as the CLAUDE_EFFORT env var.')}).optional().describe("Reasoning effort applied to the current turn. Same shape as StatusLineCommandInput.effort. Present for hooks that fire within a tool-use context (PreToolUse, PostToolUse, Stop, SubagentStop, etc.) on a model that supports the effort parameter; absent for session-lifecycle hooks and models without effort support.")})),yzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("PreToolUse"),tool_name:C.string(),tool_input:C.unknown(),tool_use_id:C.string()}))),Tzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("PermissionRequest"),tool_name:C.string(),tool_input:C.unknown(),permission_suggestions:C.array(sjt()).optional()}))),Szm=ve(()=>IT().and(C.object({hook_event_name:C.literal("PostToolUse"),tool_name:C.string(),tool_input:C.unknown(),tool_response:C.unknown(),tool_use_id:C.string(),duration_ms:C.number().optional().describe("Tool execution time in milliseconds. Excludes permission-prompt and hook time.")}))),bzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("PostToolUseFailure"),tool_name:C.string(),tool_input:C.unknown(),tool_use_id:C.string(),error:C.string(),is_interrupt:C.boolean().optional(),duration_ms:C.number().optional().describe("Tool execution time in milliseconds. Excludes permission-prompt and hook time.")}))),Ezm=ve(()=>C.object({tool_name:C.string(),tool_input:C.unknown(),tool_use_id:C.string(),tool_response:C.unknown().optional()})),Czm=ve(()=>IT().and(C.object({hook_event_name:C.literal("PostToolBatch"),tool_calls:C.array(Ezm())})).describe("Hook input for the PostToolBatch event. Fired once after every tool call in a batch has resolved, before the next model request. PostToolUse fires per-tool and may run concurrently for parallel tool calls; PostToolBatch fires exactly once with the full batch.")),Azm=ve(()=>IT().and(C.object({hook_event_name:C.literal("PermissionDenied"),tool_name:C.string(),tool_input:C.unknown(),tool_use_id:C.string(),reason:C.string()}))),Rzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("Notification"),message:C.string(),title:C.string().optional(),notification_type:C.string()}))),vzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("UserPromptSubmit"),prompt:C.string(),session_title:C.string().optional()}))),wzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("UserPromptExpansion"),expansion_type:C.enum(["slash_command","mcp_prompt"]),command_name:C.string(),command_args:C.string(),command_source:C.string().optional(),prompt:C.string()}))),kzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("SessionStart"),source:C.enum(["startup","resume","clear","compact"]),agent_type:C.string().optional(),model:C.string().optional(),session_title:C.string().optional()}))),Hzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("Setup"),trigger:C.enum(["init","maintenance"])}))),Igc=ve(()=>C.object({id:C.string(),type:C.string().describe("Friendly task-type label (e.g. 'shell', 'subagent', 'monitor', 'workflow'). Falls back to the raw discriminant for unknown types."),status:C.string(),description:C.string().describe("Free-text description. Capped at 1000 chars; clipped values append "+'an in-string "\u2026 [+N chars]" marker.'),command:C.string().optional().describe("Shell command line. Only present for 'shell' tasks. Capped at 1000 "+'chars with the same "\u2026 [+N chars]" marker.'),agent_type:C.string().optional().describe("Subagent type name. Only present for 'subagent' tasks."),server:C.string().optional().describe("MCP server name. Only present for 'monitor' / 'MCP task' tasks."),tool:C.string().optional().describe("MCP tool name. Only present for 'monitor' / 'MCP task' tasks."),name:C.string().optional().describe("Workflow name. Only present for 'workflow' tasks.")})),xgc=ve(()=>C.object({id:C.string(),schedule:C.string().describe('Cron expression, e.g. "0 9 * * 1-5".'),recurring:C.boolean().describe("False for one-shot wakeups whose cron field encodes a single fire time; true for tasks that re-fire on every match."),prompt:C.string().describe("Prompt text submitted when the cron fires. Capped at 1000 chars; "+'clipped values append an in-string "\u2026 [+N chars]" marker.')})),Izm=ve(()=>IT().and(C.object({hook_event_name:C.literal("Stop"),stop_hook_active:C.boolean(),last_assistant_message:C.string().optional().describe("Text content of the last assistant message before stopping. Avoids the need to read and parse the transcript file."),background_tasks:C.array(Igc()).optional().describe('In-flight background work (running/pending + backgrounded) registered in this session. Lets hooks distinguish "session is done" from "session is paused waiting for background work to wake it". Empty array when nothing is in flight.'),session_crons:C.array(xgc()).optional().describe("Session-scoped cron tasks (CronCreate, ScheduleWakeup, /loop) that will wake this session later. Empty array when none are scheduled.")}))),xzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("StopFailure"),error:N$o(),error_details:C.string().optional(),last_assistant_message:C.string().optional()}))),Dzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("SubagentStart"),agent_id:C.string(),agent_type:C.string()}))),Pzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("SubagentStop"),stop_hook_active:C.boolean(),agent_id:C.string(),agent_transcript_path:C.string(),agent_type:C.string(),last_assistant_message:C.string().optional().describe("Text content of the last assistant message before stopping. Avoids the need to read and parse the transcript file."),background_tasks:C.array(Igc()).optional().describe('In-flight background work (running/pending + backgrounded) registered in this session. Lets hooks distinguish "session is done" from "session is paused waiting for background work to wake it". Empty array when nothing is in flight.'),session_crons:C.array(xgc()).optional().describe("Session-scoped cron tasks (CronCreate, ScheduleWakeup, /loop) that will wake this session later. Empty array when none are scheduled.")}))),Ozm=ve(()=>IT().and(C.object({hook_event_name:C.literal("PreCompact"),trigger:C.enum(["manual","auto"]),custom_instructions:C.string().nullable()}))),Lzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("PostCompact"),trigger:C.enum(["manual","auto"]),compact_summary:C.string().describe("The conversation summary produced by compaction")}))),Mzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("TeammateIdle"),teammate_name:C.string(),team_name:C.string().describe("@deprecated Sessions have a single implicit team; this carries the session-derived team name and will be removed in a future release.")}))),Nzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("TaskCreated"),task_id:C.string(),task_subject:C.string(),task_description:C.string().optional(),teammate_name:C.string().optional(),team_name:C.string().optional().describe("@deprecated Sessions have a single implicit team; this carries the session-derived team name and will be removed in a future release.")}))),Fzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("TaskCompleted"),task_id:C.string(),task_subject:C.string(),task_description:C.string().optional(),teammate_name:C.string().optional(),team_name:C.string().optional().describe("@deprecated Sessions have a single implicit team; this carries the session-derived team name and will be removed in a future release.")}))),Bzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("Elicitation"),mcp_server_name:C.string(),message:C.string(),mode:C.enum(["form","url"]).optional(),url:C.string().optional(),elicitation_id:C.string().optional(),requested_schema:C.record(C.string(),C.unknown()).optional()})).describe("Hook input for the Elicitation event. Fired when an MCP server requests user input. Hooks can auto-respond (accept/decline) instead of showing the dialog.")),Uzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("ElicitationResult"),mcp_server_name:C.string(),elicitation_id:C.string().optional(),mode:C.enum(["form","url"]).optional(),action:C.enum(["accept","decline","cancel"]),content:C.record(C.string(),C.unknown()).optional()})).describe("Hook input for the ElicitationResult event. Fired after the user responds to an MCP elicitation. Hooks can observe or override the response before it is sent to the server.")),$zm=["user_settings","project_settings","local_settings","policy_settings","skills"],qzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("ConfigChange"),source:C.enum($zm),file_path:C.string().optional()}))),Wzm=["session_start","nested_traversal","path_glob_match","include","compact"],Gzm=["User","Project","Local","Managed"],Vzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("InstructionsLoaded"),file_path:C.string(),memory_type:C.enum(Gzm),load_reason:C.enum(Wzm),globs:C.array(C.string()).optional(),trigger_file_path:C.string().optional(),parent_file_path:C.string().optional()}))),Kzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("WorktreeCreate"),name:C.string()}))),zzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("WorktreeRemove"),worktree_path:C.string()}))),jzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("CwdChanged"),old_cwd:C.string(),new_cwd:C.string()}))),Yzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("FileChanged"),file_path:C.string(),event:C.enum(["change","add","unlink"])}))),Jzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("MessageDisplay"),turn_id:C.string().describe("UUID of the current turn."),message_id:C.string().describe("UUID of the assistant message being displayed. Stable across "+"every flush of the same message. Not the API msg_\u2026 id."),index:C.number().int().describe("Zero-based index of this delta within the message. Increments by one per flush."),final:C.boolean().describe("True on the message's last flush. Exactly one flush per message has it."),delta:C.string().describe("The newly completed lines since the prior flush. Always whole lines, except on the final flush which may end mid-line. The delta of the final flush is empty when the message ends on a newline; treat final as the end-of-message signal regardless.")})).describe("Hook input for the MessageDisplay event. Fired with each batch of newly completed lines while an assistant message streams. Display-only: the stored message and what the model sees are untouched.")),Xzm=["clear","resume","logout","prompt_input_exit","other","bypass_permissions_disabled"],Qzm=ve(()=>C.enum(Xzm)),Zzm=ve(()=>IT().and(C.object({hook_event_name:C.literal("SessionEnd"),reason:Qzm()}))),Dgc=ve(()=>C.union([yzm(),Szm(),bzm(),Czm(),Azm(),Rzm(),vzm(),wzm(),kzm(),Zzm(),Izm(),xzm(),Dzm(),Pzm(),Ozm(),Lzm(),Tzm(),Hzm(),Mzm(),Nzm(),Fzm(),Bzm(),Uzm(),qzm(),Vzm(),Kzm(),zzm(),jzm(),Yzm(),Jzm()])),ejm=ve(()=>C.object({async:C.literal(!0),asyncTimeout:C.number().optional()})),tjm=ve(()=>C.object({hookEventName:C.literal("PreToolUse"),permissionDecision:gzm().optional(),permissionDecisionReason:C.string().optional(),updatedInput:C.record(C.string(),C.unknown()).optional(),additionalContext:C.string().optional()})),njm=ve(()=>C.object({hookEventName:C.literal("UserPromptSubmit"),additionalContext:C.string().optional(),sessionTitle:C.string().optional(),suppressOriginalPrompt:C.boolean().optional().describe('When decision is "block", omit the original prompt from the block message')})),rjm=ve(()=>C.object({hookEventName:C.literal("UserPromptExpansion"),additionalContext:C.string().optional()})),ojm=ve(()=>C.object({hookEventName:C.literal("SessionStart"),additionalContext:C.string().optional(),initialUserMessage:C.string().optional(),sessionTitle:C.string().optional(),watchPaths:C.array(C.string()).optional(),reloadSkills:C.boolean().optional().describe("Re-scan skill and command directories after SessionStart hooks complete, so skills installed by the hook are available in the same session")})),sjm=ve(()=>C.object({hookEventName:C.literal("Setup"),additionalContext:C.string().optional()})),ijm=ve(()=>C.object({hookEventName:C.literal("SubagentStart"),additionalContext:C.string().optional()})),ajm=ve(()=>C.object({hookEventName:C.literal("PostToolUse"),additionalContext:C.string().optional(),updatedToolOutput:C.unknown().optional().describe("Replaces the tool output before it is sent to the model"),updatedMCPToolOutput:C.unknown().optional().describe("Replaces the output for MCP tools only. Prefer updatedToolOutput, which works for all tools")})),ljm=ve(()=>C.object({hookEventName:C.literal("PostToolBatch"),additionalContext:C.string().optional()})),cjm=ve(()=>C.object({hookEventName:C.literal("PostToolUseFailure"),additionalContext:C.string().optional()})),ujm=ve(()=>C.object({hookEventName:C.literal("Stop"),additionalContext:C.string().optional()}).describe("Hook-specific output for the Stop event. additionalContext is non-error feedback delivered to the model; the conversation continues so the model can act on it.")),djm=ve(()=>C.object({hookEventName:C.literal("SubagentStop"),additionalContext:C.string().optional()}).describe("Hook-specific output for the SubagentStop event. additionalContext is non-error feedback delivered to the subagent; the subagent continues so it can act on it.")),pjm=ve(()=>C.object({hookEventName:C.literal("PermissionDenied"),retry:C.boolean().optional()})),mjm=ve(()=>C.object({hookEventName:C.literal("Notification"),additionalContext:C.string().optional()})),fjm=ve(()=>C.object({hookEventName:C.literal("PermissionRequest"),decision:C.union([C.object({behavior:C.literal("allow"),updatedInput:C.record(C.string(),C.unknown()).optional(),updatedPermissions:C.array(sjt()).optional()}),C.object({behavior:C.literal("deny"),message:C.string().optional(),interrupt:C.boolean().optional()})])})),hjm=ve(()=>C.object({hookEventName:C.literal("CwdChanged"),watchPaths:C.array(C.string()).optional()})),gjm=ve(()=>C.object({hookEventName:C.literal("FileChanged"),watchPaths:C.array(C.string()).optional()})),_jm=ve(()=>C.object({hookEventName:C.literal("MessageDisplay"),displayContent:C.string().optional().describe("Text displayed in place of the delta. Omit (or return the delta unchanged) to display the original.")}).describe("Hook-specific output for the MessageDisplay event. Display-only: replaces the delta on screen without changing the stored message.")),yjm=ve(()=>C.object({continue:C.boolean().optional(),suppressOutput:C.boolean().optional(),stopReason:C.string().optional(),decision:C.enum(["approve","block"]).optional(),systemMessage:C.string().optional(),terminalSequence:C.string().optional().describe("A terminal escape sequence (e.g. OSC 9 / OSC 777 desktop-notification) for Claude Code to emit on your behalf. Only notification/title OSCs (0, 1, 2, 9, 99, 777) and BEL are permitted; anything else is dropped."),reason:C.string().optional(),hookSpecificOutput:C.union([tjm(),njm(),rjm(),ojm(),sjm(),ijm(),ajm(),cjm(),ljm(),ujm(),djm(),pjm(),mjm(),fjm(),Tjm(),Sjm(),hjm(),gjm(),bjm(),_jm()]).optional()})),Tjm=ve(()=>C.object({hookEventName:C.literal("Elicitation"),action:C.enum(["accept","decline","cancel"]).optional(),content:C.record(C.string(),C.unknown()).optional()}).describe("Hook-specific output for the Elicitation event. Return this to programmatically accept or decline an MCP elicitation request.")),Sjm=ve(()=>C.object({hookEventName:C.literal("ElicitationResult"),action:C.enum(["accept","decline","cancel"]).optional(),content:C.record(C.string(),C.unknown()).optional()}).describe("Hook-specific output for the ElicitationResult event. Return this to override the action or content before the response is sent to the MCP server.")),bjm=ve(()=>C.object({hookEventName:C.literal("WorktreeCreate"),worktreePath:C.string()}).describe("Hook-specific output for the WorktreeCreate event. Provides the absolute path to the created worktree directory. Command hooks print the path on stdout instead.")),n3E=ve(()=>C.union([ejm(),yjm()])),ijt=ve(()=>C.object({name:C.string().describe("Skill name (without the leading slash)"),description:C.string().describe("Description of what the skill does"),argumentHint:C.string().describe('Hint for skill arguments (e.g., "<file>")'),aliases:C.array(C.string()).optional().describe("Alternate names that resolve to this command (e.g., /cost and /stats both resolve to /usage)")}).describe("Information about an available skill (invoked via /command syntax).")),L$o=ve(()=>C.object({name:C.string().describe('Agent type identifier (e.g., "Explore")'),description:C.string().describe("Description of when to use this agent"),model:C.string().optional().describe("Model alias this agent uses. If omitted, inherits the parent's model")}).describe("Information about an available subagent that can be invoked via the Task tool.")),M$o=ve(()=>C.object({value:C.string().describe("Model identifier to use in API calls"),displayName:C.string().describe("Human-readable display name"),description:C.string().describe("Description of the model's capabilities"),supportsEffort:C.boolean().optional().describe("Whether this model supports effort levels"),supportedEffortLevels:C.array(C.enum(["low","medium","high","xhigh","max"])).optional().describe("Available effort levels for this model"),supportsAdaptiveThinking:C.boolean().optional().describe("Whether this model supports adaptive thinking (Claude decides when and how much to think)"),supportsFastMode:C.boolean().optional().describe("Whether this model supports fast mode"),supportsAutoMode:C.boolean().optional().describe("Whether this model supports auto mode"),disabled:C.boolean().optional().describe("@internal Model is visible but not selectable (e.g. a model the org's Zero Data Retention setting excludes). The human-readable reason is folded into `description`; a structured disabledReason field is the extension point if a consumer ever needs the reason separately.")}).describe("Information about an available model.")),Pgc=ve(()=>C.object({email:C.string().optional(),organization:C.string().optional(),subscriptionType:C.string().optional(),tokenSource:C.string().optional(),apiKeySource:C.string().optional(),apiProvider:C.enum(["firstParty","bedrock","vertex","foundry","anthropicAws","mantle","gateway"]).optional().describe('Active API backend. Anthropic OAuth login only applies when "firstParty"; for 3P providers the other fields are absent and auth is external (AWS creds, gcloud ADC, etc.). "gateway" means the CLI is authenticated against an enterprise gateway.')}).describe("Information about the logged in user's account.")),Ejm=ve(()=>C.union([C.string(),C.record(C.string(),Irr())])),Ogc=ve(()=>C.object({description:C.string().describe("Natural language description of when to use this agent"),tools:C.array(C.string()).optional().describe("Array of allowed tool names. If omitted, inherits all tools from parent. Note: passing 'Skill' here is deprecated \u2014 use the `skills` field instead."),disallowedTools:C.array(C.string()).optional().describe("Array of tool names to explicitly disallow for this agent. MCP server-level specs (mcp__server, mcp__server__*, mcp__*) remove every tool from the named server (or all MCP tools)."),prompt:C.string().describe("The agent's system prompt"),model:C.string().optional().describe("Model alias (e.g. 'fable', 'opus', 'sonnet', 'haiku') or full model ID (e.g. 'claude-fable-5'). If omitted or 'inherit', uses the main model"),mcpServers:C.array(Ejm()).optional(),criticalSystemReminder_EXPERIMENTAL:C.string().optional().describe("Experimental: Critical reminder added to system prompt"),skills:C.array(C.string()).optional().describe("Array of skill names to preload into the agent context"),initialPrompt:C.string().optional().describe("Auto-submitted as the first user turn when this agent is the main thread agent. Slash commands are processed. Prepended to any user-provided prompt."),maxTurns:C.number().int().positive().optional().describe("Maximum number of agentic turns (API round-trips) before stopping"),background:C.boolean().optional().describe("Run this agent as a background task (non-blocking, fire-and-forget) when invoked"),memory:C.enum(["user","project","local"]).optional().describe("Scope for auto-loading agent memory files. 'user' - ~/.claude/agent-memory/<agentType>/, 'project' - .claude/agent-memory/<agentType>/, 'local' - .claude/agent-memory-local/<agentType>/"),effort:C.union([C.enum(["low","medium","high","xhigh","max"]),C.number().int()]).optional().describe("Reasoning effort level for this agent. Either a named level or an integer"),permissionMode:XSe().optional().describe("Permission mode controlling how tool executions are handled")}).describe("Definition for a custom subagent that can be invoked via the Agent tool.")),r3E=ve(()=>C.enum(["user","project","local"]).describe("Source for loading filesystem-based settings. 'user' - Global user settings (~/.claude/settings.json). 'project' - Project settings (.claude/settings.json). 'local' - Local settings (.claude/settings.local.json).")),o3E=ve(()=>C.object({type:C.literal("local").describe("Plugin type. Currently only 'local' is supported"),path:C.string().describe("Absolute or relative path to the plugin directory"),skipMcpDiscovery:C.boolean().optional().describe("When true, the engine loads skills/hooks/agents/commands from this plugin but does NOT read its .mcp.json or manifest mcpServers. Use when the SDK host owns this plugin's MCP connections.")}).describe("Configuration for loading a plugin.")),s3E=ve(()=>C.object({canRewind:C.boolean(),error:C.string().optional(),filesChanged:C.array(C.string()).optional(),insertions:C.number().optional(),deletions:C.number().optional()}).describe("Result of a rewindFiles operation.")),Cjm=ve(()=>C.unknown()),Ajm=ve(()=>C.unknown()),Rjm=ve(()=>C.unknown()),ya=ve(()=>C.string()),Lgc=ve(()=>C.unknown()),N$o=ve(()=>C.enum(["authentication_failed","oauth_org_not_allowed","billing_error","rate_limit","overloaded","invalid_request","model_not_found","server_error","unknown","max_output_tokens"])),vjm=ve(()=>C.union([C.literal("compacting"),C.literal("requesting"),C.null()])),F$o=ve(()=>C.discriminatedUnion("kind",[C.object({kind:C.literal("human")}),C.object({kind:C.literal("channel"),server:C.string()}),C.object({kind:C.literal("peer"),from:C.string(),name:C.string().optional(),inbound_origin:C.string().optional().describe("@internal Server-asserted ingest path of the demoted delivery, copied at classification from the message-level `inbound_origin`."),senderTaskId:C.string().optional().describe("Task id of the in-process background subagent that sent this message, stamped by the harness from the sending loop (never from tool input). Absent for cross-session peers.")}),C.object({kind:C.literal("task-notification")}),C.object({kind:C.literal("coordinator")}),C.object({kind:C.literal("auto-continuation")})]).describe("Provenance of a user-role message (peer session, team lead, channel). Absent or `human` means keyboard input from the user.")),Mgc=ve(()=>C.object({type:C.literal("user"),message:Cjm(),parent_tool_use_id:C.string().nullable(),isSynthetic:C.boolean().optional(),tool_use_result:C.unknown().optional(),priority:C.enum(["now","next","later"]).optional(),origin:F$o().optional(),client_platform:C.string().optional().describe("@internal The `anthropic-client-platform` value of the client that sent this message (e.g. `ios`, `android`, `web_claude_ai`, `desktop_app`). Injected server-side by CCR ingress from the request header."),inbound_origin:C.string().optional().describe("@internal Server-asserted ingest path that produced this message. Not settable by clients."),shouldQuery:C.boolean().optional().describe("When false, the message is appended to the transcript without triggering an assistant turn. It will be merged into the next user message that does query."),timestamp:C.string().optional().describe("ISO timestamp when the message was created on the originating process. Older emitters omit it; consumers should fall back to receive time."),is_meta:C.literal(!0).optional().describe("@internal True when the message was synthesized by the loop (not user keyboard input). From internal UserMessage.isMeta."),is_visible_in_transcript_only:C.literal(!0).optional().describe("@internal True when the message is stored in the transcript but not rendered in the live UI."),is_virtual:C.literal(!0).optional().describe("@internal Display-only: rendered in the UI but filtered before API send."),is_compact_summary:C.literal(!0).optional().describe("@internal True when this user message is a compact-summary synthetic message."),summarize_metadata:C.object({messages_summarized:C.number(),user_context:C.string().optional(),direction:C.enum(["from","up_to"]).optional()}).optional().describe('@internal Metadata for "Summarize from here" / "Summarize up to here" summaries (from internal UserMessage.summarizeMetadata).'),mcp_meta:C.object({_meta:C.record(C.string(),C.unknown()).optional(),structured_content:C.record(C.string(),C.unknown()).optional()}).optional().describe("@internal MCP protocol metadata passed through to SDK consumers, never sent to the model (from internal UserMessage.mcpMeta)."),source_tool_use_id:C.string().optional().describe("@internal When this message was generated by a tool via newMessages, links it to that tool_use id."),source_tool_assistant_uuid:C.string().optional().describe("@internal For tool_result messages: UUID of the assistant message containing the matching tool_use block. Used for parentUuid assignment in transcript."),image_paste_ids:C.array(C.number()).optional().describe("@internal Paste IDs for images in this message, in order of appearance. REPL-local state \u2014 candidate for surface-side if daemon has no use for it."),plan_content:C.string().optional().describe("@internal Plan to implement (cleared-context flow). When set, the message is rendered with plan styling."),permission_mode:XSe().optional().describe("@internal Permission mode active when this message was sent (for rewind restoration)."),interrupted_message_id:C.string().optional().describe("@internal For [Request interrupted by user] markers only: the API msg_* id that Esc cancelled.")})),B$o=ve(()=>Mgc().extend({uuid:ya().optional(),session_id:C.string().optional(),subagent_type:C.string().optional().describe("Subagent type that produced this message."),task_description:C.string().optional().describe("Description of the subagent task that produced this message.")})),wjm=ve(()=>Mgc().extend({uuid:ya(),session_id:C.string(),isReplay:C.literal(!0),file_attachments:C.array(C.unknown()).optional()})),Ngc=ve(()=>C.object({type:C.literal("bash_command"),command:C.string().describe("Shell command to execute verbatim via a one-shot `/bin/sh -c` (or `pwsh`) subprocess, bypassing the model. Trust model matches the local TUI `!cmd` path (no sandbox, no per-command prompt); unlike `!cmd`, output is not appended to the conversation transcript and there is no persistent shell state across calls."),cwd:C.string().optional().describe("Working directory for the command. Falls back to the session cwd when omitted."),uuid:ya().optional(),session_id:C.string().optional()}).describe("@internal A user-initiated shell command dispatched to a one-shot shell subprocess with no model turn. Input-only \u2014 sent by CCR clients that surface a dedicated terminal UI; never emitted on stdout.")),kjm=ve(()=>C.object({status:C.enum(["allowed","allowed_warning","rejected"]),resetsAt:C.number().optional(),rateLimitType:C.enum(["five_hour","seven_day","seven_day_opus","seven_day_sonnet","seven_day_overage_included","overage"]).optional(),utilization:C.number().optional(),overageStatus:C.enum(["allowed","allowed_warning","rejected"]).optional(),overageResetsAt:C.number().optional(),overageDisabledReason:C.enum(["overage_not_provisioned","org_level_disabled","org_level_disabled_until","out_of_credits","seat_tier_level_disabled","member_level_disabled","seat_tier_zero_credit_limit","group_zero_credit_limit","member_zero_credit_limit","org_service_level_disabled","no_limits_configured","fetch_error","unknown"]).optional(),isUsingOverage:C.boolean().optional(),overageInUse:C.boolean().optional(),surpassedThreshold:C.number().optional(),overagePeriodMonthly:C.object({utilization:C.number()}).optional().describe("@internal Monthly service spend-cap telemetry for the Claude-in-Slack surface (CLAUDE_IN_SLACK_V2): utilization is fraction-of-cap."),overagePeriodChannel:C.object({utilization:C.number()}).optional().describe("@internal Per-Slack-channel spend-cap telemetry for the Claude-in-Slack surface (CLAUDE_IN_SLACK_V2): utilization is fraction-of-cap. Absent when the channel has no individual cap."),errorCode:C.enum(["credits_required"]).optional(),canUserPurchaseCredits:C.boolean().optional(),hasChargeableSavedPaymentMethod:C.boolean().optional()}).describe("Rate limit information for claude.ai subscription users.")),Hjm=ve(()=>C.object({type:C.literal("assistant"),message:Ajm(),parent_tool_use_id:C.string().nullable(),error:N$o().optional(),uuid:ya(),session_id:C.string(),request_id:C.string().optional(),supersedes:C.array(ya()).optional().describe("Wire uuids of previously-delivered messages that this message replaces (refusal-fallback supersede). The list can include tombstoned tool_result frames from the refused leg, not only assistant frames. Evict the named messages on arrival and treat this frame as their canonical replacement. Idempotent with the end-of-turn model_refusal_fallback notice, whose retracted_message_uuids remains the complete audit record for the turn."),subagent_type:C.string().optional().describe("Subagent type that produced this message."),task_description:C.string().optional().describe("Description of the subagent task that produced this message."),tool_use_meta:C.array(C.object({id:C.string(),display_name:C.string(),server_display_name:C.string().optional(),icon_url:C.string().optional()})).optional().describe("@internal Display metadata for this message's tool_use blocks, keyed by block id. display_name is the MCP server's `tool.annotations.title` when provided, otherwise a readable transform of the wire name; server_display_name is the MCP server's own display name; icon_url is the MCP server's directory icon URL (claude.ai connectors only). Omitted for blocks whose display label equals the wire name (built-in tools). Wrapper-level sibling \u2014 never inside `message.content` \u2014 so it is not replayed to the model."),timestamp:C.string().optional().describe("@internal ISO timestamp when this message was created (from internal AssistantMessage.timestamp)."),is_meta:C.literal(!0).optional().describe("@internal True when the message was synthesized by the loop (not a model response)."),is_virtual:C.literal(!0).optional().describe("@internal Display-only: rendered in the UI but filtered before API send."),is_api_error_message:C.boolean().optional().describe("@internal True when this assistant message wraps an API error (from internal AssistantMessage.isApiErrorMessage)."),api_error_status:C.number().optional().describe("@internal HTTP status code of the API error when is_api_error_message is true."),api_error:C.literal("max_output_tokens").optional().describe("@internal API error code when is_api_error_message is true."),error_details:C.string().optional().describe("@internal Raw API error message \u2014 preserves details (e.g. prompt-too-long token counts) that user-facing content discards."),advisor_model:C.string().optional().describe("@internal Advisor model that produced this message, when applicable."),attribution_agent:C.string().optional().describe("@internal Attribution stamp: agent name parsed from querySource (see messageAttribution.ts). May overlap with subagent_type."),attribution_skill:C.string().optional().describe("@internal Skill that produced this message."),attribution_plugin:C.string().optional().describe("@internal Plugin that produced this message."),attribution_mcp_server:C.string().optional().describe("@internal MCP server that produced this message."),attribution_mcp_tool:C.string().optional().describe("@internal MCP tool that produced this message.")})),Ijm=ve(()=>C.object({type:C.literal("rate_limit_event"),rate_limit_info:kjm(),uuid:ya(),session_id:C.string()}).describe("Rate limit event emitted when rate limit info changes.")),Fgc=ve(()=>C.object({tool_name:C.string(),tool_use_id:C.string(),tool_input:C.record(C.string(),C.unknown())})),xjm=ve(()=>C.object({id:C.string(),name:C.string(),input:C.record(C.string(),C.unknown())})),Bgc=ve(()=>C.enum(tVl).describe("Why the query loop terminated. Unset when the loop was bypassed (local slash command) or interrupted externally (budget/retry limits checked between yields).")),Djm=ve(()=>C.object({type:C.literal("result"),subtype:C.literal("success"),duration_ms:C.number(),duration_api_ms:C.number(),ttft_ms:C.number().optional(),ttft_stream_ms:C.number().optional(),time_to_request_ms:C.number().optional(),time_to_request_from_spawn_ms:C.number().optional(),warm_spare_claimed:C.boolean().optional(),time_origin_ms:C.number().optional(),is_error:C.boolean(),api_error_status:C.number().nullable().optional(),num_turns:C.number(),result:C.string(),stop_reason:C.string().nullable(),total_cost_usd:C.number(),usage:Lgc(),modelUsage:C.record(C.string(),krr()),permission_denials:C.array(Fgc()),structured_output:C.unknown().optional(),deferred_tool_use:xjm().optional(),terminal_reason:Bgc().optional(),fast_mode_state:ajt().optional(),origin:F$o().optional(),uuid:ya(),session_id:C.string()})),Pjm=ve(()=>C.object({type:C.literal("result"),subtype:C.enum(["error_during_execution","error_max_turns","error_max_budget_usd","error_max_structured_output_retries"]),duration_ms:C.number(),duration_api_ms:C.number(),is_error:C.boolean(),num_turns:C.number(),stop_reason:C.string().nullable(),total_cost_usd:C.number(),usage:Lgc(),modelUsage:C.record(C.string(),krr()),permission_denials:C.array(Fgc()),errors:C.array(C.string()),terminal_reason:Bgc().optional(),fast_mode_state:ajt().optional(),origin:F$o().optional(),uuid:ya(),session_id:C.string()})),Ojm=ve(()=>C.union([Djm(),Pjm()])),Ugc=ve(()=>C.object({file:C.string().optional().describe("Path to the settings file that failed to parse or validate."),path:C.string().describe("Dot-notation path to the field with the error, or empty string for whole-file errors."),message:C.string().describe("Human-readable error message.")}).describe("A settings file parse or validation error. When a settings.json file fails to parse (invalid JSON, JSON comments, schema mismatch), the file is skipped and any rules it contained \u2014 including permission allow/deny lists \u2014 are not applied.")),Ljm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("init"),agents:C.array(C.string()).optional(),apiKeySource:izm(),betas:C.array(C.string()).optional(),claude_code_version:C.string(),cwd:C.string(),tools:C.array(C.string()),mcp_servers:C.array(C.object({name:C.string(),status:C.string()})),model:C.string(),permissionMode:XSe(),slash_commands:C.array(C.string()),output_style:C.string(),skills:C.array(C.string()),plugins:C.array(C.object({name:C.string(),path:C.string(),source:C.string().optional().describe('@internal Plugin source identifier in "name\\@marketplace" format. Sentinels: "name\\@inline" for --plugin-dir, "name\\@builtin" for built-in plugins.')})),plugin_errors:C.array(C.object({plugin:C.string(),type:C.string(),message:C.string()})).optional().describe("@internal Plugin load-time errors (e.g., unsatisfied dependency version). Affected plugins are demoted and absent from `plugins[]`. The key is omitted when there are no errors; CI can fail on `(plugin_errors?.length ?? 0) > 0`."),plugin_warnings:C.array(C.object({plugin:C.string(),type:C.string(),message:C.string()})).optional().describe("@internal Plugin authoring feedback (e.g., a default folder shadowed by a manifest key). When `plugin` matches an entry in `plugins[]`, that plugin loaded and the warning is advisory; warnings with a synthetic `plugin` source (no matching `plugins[]` entry, e.g. workspace-level suppression notices) describe content that did NOT load. The key is omitted when there are no warnings."),fast_mode_state:ajt().optional(),analytics_disabled:C.boolean().optional().describe("@internal True when the CLI has analytics/telemetry disabled (privacy level, DO_NOT_TRACK, or 3P provider). IDE clients use this to hide per-message thumbs feedback UI since the rating event would be a no-op."),product_feedback_disabled:C.boolean().optional().describe("@internal True when the org's allow_product_feedback policy is false (ZDR/HIPAA). IDE clients use this to hide feedback surfaces (thumbs, session survey) whose events the CLI would drop at the proxy boundary anyway."),memory_paths:C.object({auto:C.string().optional(),team:C.string().optional()}).optional().describe("@internal Absolute directory paths for the auto-memory and team-memory stores. Lets SDK renderers classify Read/Write/Edit tool calls on these paths as memory operations without re-implementing CLI path detection."),uuid:ya(),session_id:C.string()})),Mjm=ve(()=>C.object({type:C.literal("stream_event"),event:Rjm(),parent_tool_use_id:C.string().nullable(),uuid:ya(),session_id:C.string(),ttft_ms:C.number().optional()})),Njm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("compact_boundary"),compact_metadata:C.object({trigger:C.enum(["manual","auto"]),pre_tokens:C.number(),post_tokens:C.number().optional(),duration_ms:C.number().optional(),user_context:C.string().optional().describe('@internal User-provided focus text for manual "summarize from here".'),messages_summarized:C.number().optional().describe("@internal Count of messages the compaction summarized."),precomputed:C.boolean().optional().describe("@internal The summary was generated in the background at the autocompact threshold and swapped in when prompt-too-long fired; duration_ms measures user-wait from that point."),pre_compact_discovered_tools:C.array(C.string()).optional().describe("@internal Deferred-tool names discovered before this compaction. extractDiscoveredToolNames reads this back on the next turn so the tool-schema filter keeps including them after the tool_reference-carrying messages were summarized away."),preserved_segment:C.object({head_uuid:ya(),anchor_uuid:ya(),tail_uuid:ya()}).optional().describe("Relink info for messagesToKeep. Loaders splice the preserved segment at anchor_uuid (summary for suffix-preserving, boundary for prefix-preserving partial compact) so resume includes preserved content. Unset when compaction summarizes everything (no messagesToKeep)."),preserved_messages:C.object({anchor_uuid:ya(),uuids:C.array(ya()),all_uuids:C.array(ya()).optional().describe("@internal Unfiltered messagesToKeep UUIDs. uuids is the on-disk subset (messages recordTranscript writes); all_uuids is the in-memory superset including non-loggable messages an in-process surface still holds for the next turn's API input. Absent from older producers.")}).optional().describe("Ordered messagesToKeep UUIDs. Supersedes preserved_segment \u2014 "+"readers look up each UUID directly and relink uuids[i] to uuids[i-1] (uuids[0] to anchor_uuid) instead of walking the parentUuid chain. Unset when compaction summarizes everything.")}),logical_parent_uuid:ya().nullable().optional().describe("@internal uuid of the last pre-compact message \u2014 the backpointer "+"forkSession follows across the compaction break. Distinct from the session-file chain parent (which is the post-compact summary). Absent from older producers."),uuid:ya(),session_id:C.string()})),Fjm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("status"),status:vjm(),permissionMode:XSe().optional(),compact_result:C.enum(["success","failed"]).optional(),compact_error:C.string().optional(),uuid:ya(),session_id:C.string()})),U$o=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("post_turn_summary"),summarizes_uuid:C.string(),status_category:C.string(),status_detail:C.string(),needs_action:C.string(),uuid:ya(),session_id:C.string()}).describe("@internal Background post-turn summary emitted after each assistant turn. summarizes_uuid points to the assistant message this summarizes.")),$$o=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("task_summary"),detail:C.string().nullable(),uuid:ya(),session_id:C.string()}).describe("@internal Mid-turn progress line from the debounced classifier. Mirrors external_metadata.task_summary so non-CCR consumers (desktop LocalSessionManager) see the same live phrase. detail is null on the idle clear.")),Bjm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("informational"),content:C.string(),level:C.enum(["info","notice","suggestion","warning"]).describe("Render level. 'info' shows only in transcript mode; 'notice' renders in inactive gray; 'suggestion' and 'warning' are more prominent."),tool_use_id:C.string().optional().describe("Dedupes progress messages for the same tool use."),prevent_continuation:C.boolean().optional().describe("When true, execution stops after this message (e.g. a Stop hook denied continuation)."),uuid:ya(),session_id:C.string()}).describe("Generic text banner emitted by the loop \u2014 non-error status lines, hook feedback (e.g. a UserPromptSubmit hook's block reason), slash-command output. Hosts render `content` as plaintext at the given level.")),i3E=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("permission_retry"),content:C.string(),commands:C.array(C.string()).describe("Display names of the commands that were allowed."),uuid:ya(),session_id:C.string()}).describe("@internal Emitted when tool execution retries after a permission-mode change allowed previously-denied commands. REPL renders a 'retrying with <commands>' banner. From internal SystemMessage 'permission_retry'.")),a3E=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("stop_hook_summary"),hook_count:C.number(),hook_infos:C.array(C.object({command:C.string(),prompt_text:C.string().optional(),duration_ms:C.number().optional()})),hook_errors:C.array(C.string()),hook_additional_context:C.array(C.string()).optional().describe("Non-error feedback from hookSpecificOutput.additionalContext \u2014 kept separate from hook_errors so the sanctioned feedback channel is not labeled an error. Absent in sessions persisted before this field existed."),prevented_continuation:C.boolean(),stop_reason:C.string().optional(),has_output:C.boolean(),level:C.enum(["info","notice","suggestion","warning"]),tool_use_id:C.string().optional(),hook_label:C.string().optional(),total_duration_ms:C.number().optional(),uuid:ya(),session_id:C.string()}).describe("@internal Summary of Stop/SubagentStop hook execution at turn end \u2014 which hooks ran, their output, and whether any prevented continuation. From internal SystemMessage 'stop_hook_summary'.")),l3E=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("memory_saved"),written_paths:C.array(C.string()),team_count:C.number().optional(),verb:C.string().optional().describe('Renders as "<verb> N memories". Defaults to "Saved".'),uuid:ya(),session_id:C.string()}).describe("@internal Confirmation that the memory subsystem wrote to the listed paths. REPL renders a '<verb> N memories' banner. From internal SystemMessage 'memory_saved'.")),c3E=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("agents_killed"),uuid:ya(),session_id:C.string()}).describe("@internal Emitted when background agents are terminated (e.g. on interrupt). REPL renders an 'agents killed' banner. From internal SystemMessage 'agents_killed'.")),u3E=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("away_summary"),content:C.string(),uuid:ya(),session_id:C.string()}).describe("@internal Summary of what happened while the user was away (background tasks completed, notifications accumulated). From internal SystemMessage 'away_summary'.")),d3E=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("thinking"),content:C.string(),uuid:ya(),session_id:C.string()}).describe("@internal Rendered thinking content (the text itself, not the running token estimate \u2014 that is SDKThinkingTokensMessage). From internal SystemMessage 'thinking'.")),q$o=ve(()=>C.object({type:C.literal("transcript_mirror"),filePath:C.string(),entries:C.array(C.unknown())}).describe("@internal Emitted after each successful local transcript write. The parent peels these off the stdout stream and batches them to the SessionStore adapter. Not exposed to public SDK consumers.")),Ujm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("mirror_error"),error:C.string(),key:C.object({projectKey:C.string(),sessionId:C.string(),subpath:C.string().optional()}),uuid:ya(),session_id:C.string()}).describe("Emitted when SessionStore.append() rejects or times out for a transcript-mirror batch after bounded retry (3 attempts with short backoff; timeouts are not retried). The batch is then dropped; this surfaces the failure so consumers are not silent on data loss.")),$jm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("api_retry"),attempt:C.number(),max_retries:C.number(),retry_delay_ms:C.number(),error_status:C.number().nullable(),error:N$o(),uuid:ya(),session_id:C.string()}).describe("Emitted when an API request fails with a retryable error and will be retried after a delay. error_status is null for connection errors (e.g. timeouts) that had no HTTP response.")),qjm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("model_refusal_fallback"),trigger:C.literal("refusal"),direction:C.enum(["retry","revert","sticky"]),original_model:C.string(),fallback_model:C.string(),request_id:C.string().nullable(),api_refusal_category:C.string().nullable().optional().describe("The refusal category ('cyber', 'bio', \u2026): stop_details.category from the refused API response (client lane), or the fallback block's server-gated trigger.category (server lane). Open string \u2014 new categories ship on the wire ahead of schema updates. null when neither source carried a category (normal, not an error). Absent when emitted by an older CLI."),api_refusal_explanation:C.string().nullable().optional().describe("stop_details.explanation from the refused API response (client lane only \u2014 the server-lane trigger carries no explanation). Unstable human prose \u2014 display only, never parse. null/absent when the response carried none, and always null on server-lane banners."),retracted_message_uuids:C.array(C.string()).optional().describe("Wire uuids of the messages this fallback retracted \u2014 the refused partial as the consumer received it (one uuid per normalized SDK message; multi-block messages carry per-block derived uuids) plus any tombstoned tool_results. Emitted AFTER the retraction, so this is a resolution-time eviction signal: remove these messages from transcript state on receipt. Eviction is idempotent \u2014 unknown or already-removed uuids are a no-op. Absent when emitted by an older CLI."),content:C.string(),uuid:ya(),session_id:C.string()}).describe('Emitted when the primary model ends the stream with stop_reason "refusal" and the turn is retried once on a fallback model with the swap made persistent for the session (direction: "retry"). "revert" and "sticky" are retained in the enum for SDK-consumer compat and are no longer emitted.')),p3E=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("model_fallback"),trigger:C.enum(["model_not_found","permission_denied","overloaded","server_error","last_resort","model_blocked"]),original_model:C.string(),fallback_model:C.string(),content:C.string(),uuid:ya(),session_id:C.string()}).describe('@internal Emitted when the current turn is switched to the configured fallback model because the primary model failed (trigger "model_not_found": model retired/unknown; "permission_denied": org lacks access; "overloaded": repeated 529s; "server_error": retryable 5xx pivot; "last_resort": non-retryable error on the primary; "model_blocked": primary disabled by the per-model kill switch). Turn-scoped \u2014 the primary is re-tried on the next user turn. Not yet in the public SDKMessage union.')),m3E=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("model_consent_fallback"),choice:C.enum(["consent","switch_default","cancelled"]).describe("The consent-prompt answer (or no-dialog collapse) that produced the swap. 'consent' appears here only when the gate could not honor it (e.g. usage credits did not end up provisioned \u2014 the loop never enables billing from a bare wire reply)."),original_model:C.string(),fallback_model:C.string(),persisted_as_default:C.boolean().describe("True when the decline also rewrote the saved default model (explicit switch_default with the consent-gated model as the saved default)."),content:C.string(),uuid:ya(),session_id:C.string()}).describe("@internal Emitted when a pre-send model consent gate swaps the session off the requested model (consent declined, dismissed, or given without the required entitlement ending up provisioned). Currently emitted by the Fable 5 usage-credit gate (`fable_overage_consent_prompt`). Session-scoped \u2014 the swap persists for the session, and additionally as the saved default when persisted_as_default is true. Absence of this message after the consent dialog resolves means the session stayed on the requested model. Not yet in the public SDKMessage union.")),f3E=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("file_snapshot"),content:C.string(),snapshot_files:C.array(C.object({key:C.string().describe("Identifier for the file type (e.g. 'plan', 'todo')."),path:C.string().describe("Original file path (for debugging)."),content:C.string()})),uuid:ya(),session_id:C.string()}).describe("@internal Snapshot of session files (plan, todo) captured for rewind. From internal SystemMessage 'file_snapshot'.")),h3E=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("scheduled_task_fire"),content:C.string(),uuid:ya(),session_id:C.string()}).describe("@internal Emitted when a scheduled task (cron) fires. content is the render text. From internal SystemMessage 'scheduled_task_fire'.")),g3E=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("turn_duration"),duration_ms:C.number(),budget_tokens:C.number().optional().describe("Output tokens spent this turn toward the token budget."),budget_limit:C.number().optional().describe("Turn token-budget ceiling."),budget_nudges:C.number().optional().describe("Budget-nudge count this turn."),message_count:C.number().optional().describe("In-memory message count at turn end; used by resume-consistency telemetry to detect write\u2192load round-trip drift."),pending_background_agent_count:C.number().optional().describe("Background Agent-tool runs still in flight when the turn finished."),pending_workflow_count:C.number().optional().describe("Workflow-tool runs still in flight when the turn finished."),uuid:ya(),session_id:C.string()}).describe("@internal Per-turn wall-clock duration plus budget and pending-background-work counts. REPL renders the 'Done in Ns' / 'Waiting for N agents' line. From internal SystemMessage 'turn_duration'.")),_3E=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("api_error"),error:C.object({message:C.string(),status:C.number().optional(),request_id:C.string().optional(),formatted:C.string().describe("Human-readable display string for the error."),connection:C.object({code:C.string(),message:C.string(),is_ssl_error:C.boolean()}).nullable().describe("errno/SSL code extracted from the cause chain; null when absent."),is_network_down:C.boolean(),rate_limits:C.object({resets_at:C.number().optional(),rate_limit_type:C.string().optional()}).nullable().describe("Quota-429 headers surfaced by the retry banner; null when not a quota 429.")}).describe("Plain-data snapshot of the APIError \u2014 the class instance cannot cross the wire."),retry_in_ms:C.number(),retry_attempt:C.number(),max_retries:C.number(),uuid:ya(),session_id:C.string()}).describe("@internal Retryable-API-error frame carrying the plain-data error snapshot and retry counters. REPL renders the retry banner from this. Wire twin is SDKAPIRetryMessage ('api_retry'). From internal SystemMessage 'api_error'.")),Wjm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("local_command_output"),content:C.string(),uuid:ya(),session_id:C.string()}).describe("Output from a local slash command (e.g. /voice, /usage). Displayed as assistant-style text in the transcript.")),Gjm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("hook_started"),hook_id:C.string(),hook_name:C.string(),hook_event:C.string(),uuid:ya(),session_id:C.string()})),Vjm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("hook_progress"),hook_id:C.string(),hook_name:C.string(),hook_event:C.string(),stdout:C.string(),stderr:C.string(),output:C.string(),uuid:ya(),session_id:C.string()})),Kjm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("hook_response"),hook_id:C.string(),hook_name:C.string(),hook_event:C.string(),output:C.string(),stdout:C.string(),stderr:C.string(),exit_code:C.number().optional(),outcome:C.enum(["success","error","cancelled"]),uuid:ya(),session_id:C.string()})),zjm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("plugin_install"),status:C.enum(["started","installed","failed","completed"]),name:C.string().optional(),error:C.string().optional(),uuid:ya(),session_id:C.string()}).describe("Headless plugin installation progress (CLAUDE_CODE_SYNC_PLUGIN_INSTALL). started/completed bracket the whole install; installed/failed carry a per-marketplace name.")),jjm=ve(()=>C.object({type:C.literal("tool_progress"),tool_use_id:C.string(),tool_name:C.string(),parent_tool_use_id:C.string().nullable(),elapsed_time_seconds:C.number(),task_id:C.string().optional(),uuid:ya(),session_id:C.string()})),Yjm=ve(()=>C.object({type:C.literal("auth_status"),isAuthenticating:C.boolean(),output:C.array(C.string()),error:C.string().optional(),uuid:ya(),session_id:C.string()})),Jjm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("files_persisted"),files:C.array(C.object({filename:C.string(),file_id:C.string()})),failed:C.array(C.object({filename:C.string(),error:C.string()})),processed_at:C.string(),uuid:ya(),session_id:C.string()})),Xjm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("task_notification"),task_id:C.string(),tool_use_id:C.string().optional(),status:C.enum(["completed","failed","stopped"]),output_file:C.string(),summary:C.string(),usage:C.object({total_tokens:C.number(),tool_uses:C.number(),duration_ms:C.number()}).optional(),skip_transcript:C.boolean().optional(),uuid:ya(),session_id:C.string()})),Qjm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("task_started"),task_id:C.string(),tool_use_id:C.string().optional(),description:C.string(),subagent_type:C.string().optional().describe("Subagent type for Task tool subagents."),task_type:C.string().optional(),workflow_name:C.string().optional().describe("meta.name from the workflow script (e.g. 'spec'). Only set when task_type is 'local_workflow'."),prompt:C.string().optional(),skip_transcript:C.boolean().optional().describe("Ambient/housekeeping task. Consumers should hide this from the inline transcript; it may still appear in a tasks panel."),uuid:ya(),session_id:C.string()})),Zjm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("task_updated"),task_id:C.string(),patch:C.object({status:C.enum(["pending","running","completed","failed","killed","paused"]).optional(),description:C.string().optional(),end_time:C.number().optional(),total_paused_ms:C.number().optional(),error:C.string().optional(),is_backgrounded:C.boolean().optional()}).describe("Wire-safe subset of TaskState fields that changed. Excludes abortController, messages, result. Clients merge into their local task map."),uuid:ya(),session_id:C.string()})),eYm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("session_state_changed"),state:C.enum(["idle","running","requires_action"]),uuid:ya(),session_id:C.string()}).describe("Mirrors notifySessionStateChanged. 'idle' fires after heldBackResult flushes and the bg-agent do-while exits \u2014 authoritative turn-over signal.")),tYm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("worker_shutting_down"),reason:C.string().describe("Short snake_case reason set by the host CLI (not user input), e.g. 'host_exit', 'remote_control_disabled'."),uuid:ya(),session_id:C.string()}).describe("Emitted by the bridge on opt-in graceful worker teardown (only when the teardown caller supplied a reason), before the heartbeat stops, so remote clients can show why the worker went away instead of waiting for heartbeat timeout. Absence is NOT a dead-host signal: handoffs (/update, /teleport, respawn), auto-disable, mode transitions, and internal fatal-error paths emit nothing by design. A dead host (battery, OOM, kill -9) never reaches teardown and never sends this either. NOTE: this event lands in the durable per-session event stream \u2014 a session that is later resumed may carry historical instances mid-stream. Clients MUST treat it as a live-tail signal only (honored when no further activity follows), not a one-shot session-lifetime fact. CC-2656.")),nYm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("commands_changed"),commands:C.array(ijt()),uuid:ya(),session_id:C.string()}).describe("Fire-and-forget push of the full slash-command list after a mid-session change (e.g. skills discovered dynamically as the agent works in a subdirectory). Clients should REPLACE their cached command list with this payload: supportedCommands() is captured once at initialize and never reflects mid-session changes, so a client re-fetch would return the stale init list.")),rYm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("notification"),key:C.string(),text:C.string(),priority:C.enum(["low","medium","high","immediate"]),color:C.string().optional(),timeout_ms:C.number().optional(),uuid:ya(),session_id:C.string()}).describe("Loop-side text notification. Mirrors the interactive REPL notification queue (key/priority/timeout). JSX notifications are not emitted on this channel.")),oYm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("task_progress"),task_id:C.string(),tool_use_id:C.string().optional(),description:C.string(),subagent_type:C.string().optional().describe("Subagent type for Task tool subagents."),usage:C.object({total_tokens:C.number(),tool_uses:C.number(),duration_ms:C.number()}),last_tool_name:C.string().optional(),summary:C.string().optional(),uuid:ya(),session_id:C.string()})),sYm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("thinking_tokens"),estimated_tokens:C.number(),estimated_tokens_delta:C.number(),uuid:ya(),session_id:C.string()}).describe("Live thinking-token estimate, digested from thinking_delta.estimated_tokens during the redacted-thinking phase (where the API otherwise streams only pings). estimated_tokens is the running total for the current thinking block; estimated_tokens_delta is the increment carried by this frame. Approximate progress for spinners/pills, not the authoritative billed output_tokens.")),iYm=ve(()=>C.object({type:C.literal("tool_use_summary"),summary:C.string(),preceding_tool_use_ids:C.array(C.string()),uuid:ya(),session_id:C.string(),timestamp:C.string().optional().describe("@internal ISO timestamp when the summary was created on the originating process. From internal ToolUseSummaryMessage.timestamp.")})),aYm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("memory_recall"),mode:C.enum(["select","synthesize"]).describe("How memories were surfaced: 'select' returns full file bodies chosen by the parallel selector; 'synthesize' returns a Sonnet-authored paragraph distilled from many tiny memories."),memories:C.array(C.object({path:C.string().describe("Absolute path to the memory file, a synthesis sentinel of the form `<synthesis:DIR>` when mode is 'synthesize', or an https URL when scope is 'organization'."),scope:C.enum(["personal","team","organization"]),content:C.string().optional().describe("The surfaced memory body. Always present for 'synthesize' mode and 'organization' scope (neither has an on-disk path to lazy-load from); absent for file-backed 'select' entries (renderers lazy-load from path).")})),uuid:ya(),session_id:C.string()}).describe('Emitted when the memory recall supervisor surfaces relevant memories into the turn. Mirrors the CLI relevant_memories attachment so SDK renderers can show "Recalled from memory" inline.')),lYm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("elicitation_complete"),mcp_server_name:C.string(),elicitation_id:C.string(),uuid:ya(),session_id:C.string()}).describe("Emitted when an MCP server confirms that a URL-mode elicitation is complete.")),cYm=ve(()=>C.object({type:C.literal("system"),subtype:C.literal("permission_denied"),tool_name:C.string(),tool_use_id:C.string(),agent_id:C.string().optional().describe("Subagent ID when the denied tool call originated inside a subagent. Mirrors can_use_tool for host-side routing."),decision_reason_type:C.string().optional().describe("Discriminator from PermissionDecisionReason (e.g. 'classifier', 'asyncAgent', 'mode', 'rule')."),decision_reason:C.string().optional().describe("Human-readable reason from the deciding component, when available."),message:C.string().describe("The rejection message returned to the model in the tool_result."),uuid:ya(),session_id:C.string()}).describe("Emitted when a tool call is auto-denied without an interactive permission prompt (e.g. auto-mode classifier, dontAsk mode, headless-agent auto-deny, or a deny rule). The 'ask' path surfaces via a can_use_tool control_request; this event covers the 'deny' short-circuit in canUseTool so SDK hosts can render the denial instead of only seeing an is_error tool_result. PreToolUse hook denies bypass canUseTool and are not covered here.")),uYm=ve(()=>C.object({type:C.literal("prompt_suggestion"),suggestion:C.string(),uuid:ya(),session_id:C.string()}).describe("Predicted next user prompt, emitted after each turn when promptSuggestions is enabled.")),y3E=ve(()=>C.object({type:C.literal("attachment"),attachment:C.unknown().describe("Internal Attachment discriminated union (at-mentioned files, IDE selections, pasted images, structured output, deferred tool-use). Wire shape pending a dedicated SDKAttachment schema."),timestamp:C.string(),uuid:ya(),session_id:C.string()}).describe("@internal Emitted when the engine yields an AttachmentMessage into the turn stream. Carries user-attached content (at-mentioned files, IDE selections, pasted media) and loop-attached data (structured output, deferred tool-use payloads). SDKResultMessage.structured_output and .deferred_tool_use are derived from these frames. From internal QueryEvent 'attachment'.")),T3E=ve(()=>C.object({type:C.literal("tombstone"),message:C.unknown().describe("The internal Message being tombstoned. Wire shape pending a dedicated schema."),uuid:ya(),session_id:C.string()}).describe("@internal Emitted when a previously-yielded message is superseded or removed from the transcript (e.g., streaming\u2192non-streaming fallback removes a partial orphan). Consumers that render or persist the stream should remove the referenced message. From internal QueryEvent 'tombstone'.")),S3E=ve(()=>C.object({type:C.literal("conversation_reset"),new_conversation_id:ya(),uuid:ya(),session_id:C.string()}).describe("@internal Emitted by /clear, plan-mode exit, and fresh-session flows. The surface should mount a fresh transcript under new_conversation_id and reset any cached session title. From internal QueryEvent 'conversation_reset'.")),b3E=ve(()=>C.object({type:C.literal("api_metrics"),event:C.discriminatedUnion("type",[C.object({type:C.literal("start"),ttft_ms:C.number(),id:C.string().optional(),message_id:C.string().optional()}),C.object({type:C.literal("end"),output_tokens:C.number(),id:C.string().optional()}),C.object({type:C.literal("content_block_start"),id:C.string().optional()}),C.object({type:C.literal("thinking_progress"),estimated_tokens_delta:C.number(),id:C.string().optional()}),C.object({type:C.literal("thinking_signature"),chars:C.number(),id:C.string().optional()})]).describe("Per-API-call OTPS/TTFT lifecycle event. Optional id correlates parallel subagent start/end; serial callers omit it."),uuid:ya(),session_id:C.string()}).describe("@internal Emitted when a subagent's API call reports TTFT or output_tokens for OTPS (output-tokens-per-second) metering. From internal QueryEvent 'api_metrics' (ApiMetricsLifecycleEvent).")),E3E=ve(()=>C.object({type:C.literal("os_notification"),message:C.string(),notification_type:C.string(),uuid:ya(),session_id:C.string()}).describe("@internal Emitted when a tool (PushNotificationTool, the Computer Use wrapper) or turn-end cleanup requests a native OS notification. The surface dispatches to its platform notification channel (iTerm2/Kitty/Ghostty/bell in the terminal; native IPC for desktop/IDE). From internal QueryEvent 'os_notification'.")),C3E=ve(()=>C.object({type:C.literal("apply_flag_settings"),settings:C.record(C.string(),C.unknown()).describe("Shallow-merge flag-settings patch \u2014 same shape as SDKControlApplyFlagSettingsRequest.settings."),uuid:ya(),session_id:C.string()}).describe("@internal Output-direction counterpart to SDKControlApplyFlagSettingsRequest. Emitted when slash commands that toggle flag settings request a batched write that the surface applies to its AppState. From internal QueryEvent 'apply_flag_settings'.")),A3E=ve(()=>C.object({type:C.literal("command_lifecycle"),command_uuid:C.string().describe("The queued command's uuid. Renamed from Engine 'uuid' to avoid collision with the universal message uuid field."),state:C.enum(["started","completed"]),uuid:ya(),session_id:C.string()}).describe("@internal Emitted when a queued slash command starts draining ('started') or finishes ('completed'). Remote transports (mobile/desktop bridge) forward the 'completed' ACK so the client knows its queued command was processed. From internal QueryEvent 'command_lifecycle'.")),R3E=ve(()=>C.object({type:C.literal("set_expanded_view"),expanded_view:C.enum(["none","tasks","teammates"]),uuid:ya(),session_id:C.string()}).describe("@internal Hint to expand a side panel. Enum is a superset of AppState.expandedView for back-compat; do not narrow.")),v3E=ve(()=>C.object({type:C.literal("active_goal"),value:C.object({condition:C.string(),iterations:C.number(),set_at:C.number(),tokens_at_start:C.number(),last_reason:C.string().optional()}).nullable(),uuid:ya(),session_id:C.string()}).describe("@internal Emitted when the user's /goal Stop hook reports met (clears) or not-yet-met (bumps iterations + last_reason). Any surface with a goal indicator re-renders from this. value is null when the goal is cleared. From internal QueryEvent 'active_goal'.")),w3E=ve(()=>C.object({type:C.literal("set_in_progress_tool_use_ids"),op:C.object({action:C.enum(["add","remove"]),ids:C.array(C.string())}),uuid:ya(),session_id:C.string()}).describe("@internal Emitted when tool execution adds/removes tool_use ids from the mid-execution set (after permission grant, before result). Surfaces use this to show which tools are running. From internal QueryEvent 'set_in_progress_tool_use_ids'.")),k3E=ve(()=>C.object({type:C.literal("hint_clears"),ids:C.array(C.string()),content_by_id:C.record(C.string(),C.string()),uuid:ya(),session_id:C.string()}).describe("@internal Emitted when the server-side context-hint reject path reports cleared tool_use ids after a retry/fallback. The surface re-runs clearToolResultsById on its message list so subsequent turns match the API's view. From internal QueryEvent 'hint_clears'.")),H3E=ve(()=>C.object({type:C.literal("interruptible_tool_in_progress"),in_progress:C.boolean(),uuid:ya(),session_id:C.string()}).describe("@internal Emitted when the set of executing tools transitions in or out of an all-interruptible state. The surface uses this to decide whether a fresh user submit should interrupt the current turn (vs. queue). From internal QueryEvent 'interruptible_tool_in_progress'.")),I3E=ve(()=>C.object({type:C.literal("open_message_selector"),uuid:ya(),session_id:C.string()}).describe("@internal Emitted by /rewind to open the message-selector overlay. Fire-and-forget \u2014 the user's selection returns through a separate channel. From internal QueryEvent 'open_message_selector'.")),x3E=ve(()=>C.object({type:C.literal("compact_progress"),event:C.discriminatedUnion("type",[C.object({type:C.literal("hooks_start"),hook_type:C.enum(["pre_compact","post_compact","session_start"])}),C.object({type:C.literal("compact_start"),hint_text:C.string().nullable().optional()}),C.object({type:C.literal("compact_end")})]).describe("In-progress compaction lifecycle event."),uuid:ya(),session_id:C.string()}).describe("@internal Emitted while compaction is running (hook phase, compact start, compact end). Distinct from system/compact_boundary, which reports the post-compaction transcript boundary after completion. From internal QueryEvent 'compact_progress' (CompactEvent Delta-track arm).")),D3E=ve(()=>C.object({type:C.literal("stream_mode"),mode:C.enum(["tool-input","tool-use","requesting","responding","thinking"]),uuid:ya(),session_id:C.string()}).describe("@internal Emitted when the engine's spinner phase changes during compaction. From internal QueryEvent 'stream_mode' (CompactEvent Delta-track arm).")),P3E=ve(()=>C.discriminatedUnion("op",[C.object({type:C.literal("response_length"),op:C.literal("add"),delta:C.number(),uuid:ya(),session_id:C.string()}),C.object({type:C.literal("response_length"),op:C.literal("reset"),uuid:ya(),session_id:C.string()})]).describe("@internal Emitted to drive the streaming-output character counter in the spinner ('add' accumulates, 'reset' zeroes on compaction-boundary swap). From internal QueryEvent 'response_length' (Delta-track).")),O3E=ve(()=>C.discriminatedUnion("phase",[C.object({type:C.literal("refusal_continuation"),phase:C.literal("begin"),salvage_text:C.string(),uuid:ya(),session_id:C.string()}),C.object({type:C.literal("refusal_continuation"),phase:C.literal("end"),uuid:ya(),session_id:C.string()})]).describe("@internal Emitted when a refusal-continuation window begins ('begin' with salvage_text to keep visible in the streaming preview) or ends ('end'). From internal QueryEvent 'refusal_continuation'.")),L3E=ve(()=>C.object({sessionId:C.string().describe("Unique session identifier (UUID)."),summary:C.string().describe("Display title for the session: custom title, auto-generated summary, or first prompt."),lastModified:C.number().describe("Last modified time in milliseconds since epoch."),fileSize:C.number().optional().describe("File size in bytes. Only populated for local JSONL storage."),customTitle:C.string().optional().describe("User-set session title via /rename."),firstPrompt:C.string().optional().describe("First meaningful user prompt in the session."),gitBranch:C.string().optional().describe("Git branch at the end of the session."),cwd:C.string().optional().describe("Working directory for the session."),tag:C.string().optional().describe("User-set session tag."),createdAt:C.number().optional().describe("Creation time in milliseconds since epoch, extracted from the first entry's timestamp.")}).describe("Session metadata returned by listSessions and getSessionInfo.")),W$o=ve(()=>C.union([Hjm(),B$o(),wjm(),Ojm(),Ljm(),Mjm(),Njm(),Fjm(),$jm(),qjm(),Wjm(),Gjm(),Vjm(),Kjm(),zjm(),jjm(),Yjm(),Xjm(),Qjm(),Zjm(),oYm(),sYm(),eYm(),tYm(),nYm(),rYm(),Jjm(),iYm(),aYm(),Ijm(),lYm(),cYm(),uYm(),Ujm(),Bjm()])),ajt=ve(()=>C.enum(["off","cooldown","on"]).describe("Fast mode state: off, in cooldown after rate limit, or actively enabled."))});
export {krr,ozm,Y9E,szm,J9E,izm,X9E,Q9E,azm,lzm,czm,Z9E,P$o,Hrr,uzm,kgc,dzm,pzm,mzm,Irr,fzm,hzm,O$o,e3E,Xyt,x$o,gzm,D$o,sjt,wgc,t3E,XSe,_zm,Hgc,IT,yzm,Tzm,Szm,bzm,Ezm,Czm,Azm,Rzm,vzm,wzm,kzm,Hzm,Igc,xgc,Izm,xzm,Dzm,Pzm,Ozm,Lzm,Mzm,Nzm,Fzm,Bzm,Uzm,$zm,qzm,Wzm,Gzm,Vzm,Kzm,zzm,jzm,Yzm,Jzm,Xzm,Qzm,Zzm,Dgc,ejm,tjm,njm,rjm,ojm,sjm,ijm,ajm,ljm,cjm,ujm,djm,pjm,mjm,fjm,hjm,gjm,_jm,yjm,Tjm,Sjm,bjm,n3E,ijt,L$o,M$o,Pgc,Ejm,Ogc,r3E,o3E,s3E,Cjm,Ajm,Rjm,ya,Lgc,N$o,vjm,F$o,Mgc,B$o,wjm,Ngc,kjm,Hjm,Ijm,Fgc,xjm,Bgc,Djm,Pjm,Ojm,Ugc,Ljm,Mjm,Njm,Fjm,U$o,$$o,Bjm,i3E,a3E,l3E,c3E,u3E,d3E,q$o,Ujm,$jm,qjm,p3E,m3E,f3E,h3E,g3E,_3E,Wjm,Gjm,Vjm,Kjm,zjm,jjm,Yjm,Jjm,Xjm,Qjm,Zjm,eYm,tYm,nYm,rYm,oYm,sYm,iYm,aYm,lYm,cYm,uYm,y3E,T3E,S3E,b3E,E3E,C3E,A3E,R3E,v3E,w3E,k3E,H3E,I3E,x3E,D3E,P3E,O3E,L3E,W$o,ajt,$gc};
