// @ts-nocheck
import {ft,b} from "../../runtime.ts";
import {zn} from "../api/0465_getOauthConfig.ts";
import {Sn,lr} from "../../vendor/m233.ts";
import {J_o,Y_o,iQa,sQa} from "../../vendor/m4303.ts";
import {JXa,y5n,Gqt,V_o} from "../config/4301_level.ts";
import {isTmuxControlMode,Po} from "../../vendor/m638.ts";
import {fL,tQa,Kqt,K_o,Vqt,nQa,F5e,N5e} from "../../vendor/m4302.ts";
import {LXa,W_o,NXa,FXa,BXa,MXa,UXa,qXa,WXa,$Xa,KXa,GXa} from "../core/4300_auth.ts";
import {$c,Ct,Ce} from "../../vendor/m197.ts";
import {Qr} from "../../vendor/m323.ts";
import {ri,Ks} from "./2235_userFacingName.ts";
import {xl,Mr} from "../../vendor/m4427.ts";
import {tn,TeamDeleteToolName} from "../config/0230_encoding.ts";
import {T5n,pmt} from "../telemetry/4302_T5n.ts";
import {ve} from "../../vendor/m461.ts";
import {C} from "../../vendor/m321.ts";
import {vBt,cto} from "./3326_servers.ts";
var uQa={};
ft(uQa,{DesignSyncTool:()=>DesignSyncTool,DesignSyncPreconditionError:()=>DesignSyncPreconditionError});
function i9p(e){let t=s9p[e.method],n=t.present.filter((o)=>e[o]===void 0),r=t.nonEmpty.filter((o)=>{let s=e[o];return s===void 0||Array.isArray(s)&&s.length===0});return[...n,...r]}
function l9p(e){return e==="list_projects"||e==="get_project"||e==="list_files"||e==="get_file"||e==="report_validate"}
function aQa(e){if(!e)return"?";return e.length>12?`${e.slice(0,8)}\u2026`:e}
function X_o(e){switch(e?.method){case"list_projects":return"List design-system projects";case"get_project":return"Read project metadata";case"list_files":return"List project files";case"get_file":return e.path?`Read ${e.path}`:"Read file";case"finalize_plan":{let t=e.writes?.length??0,n=e.deletes?.length??0;return`Upload design system (${n>0?`${t} to upload, ${n} to delete`:`${t} to upload`})`}case"write_files":{let t=e.files?.length??0,n=zn(e.files??[],(o)=>o.localPath!==void 0),r=n>0&&n<t?` (${n} from disk, ${t-n} inline)`:n===t&&t>0?" from disk":"";return`Write ${t} ${Sn(t,"file")}${r}`}case"delete_files":return`Delete ${e.paths?.length??0} ${Sn(e.paths?.length??0,"file")}`;case"register_assets":return`Register ${e.assets?.length??0} ${Sn(e.assets?.length??0,"asset card")}`;case"unregister_assets":return`Unregister ${e.paths?.length??0} ${Sn(e.paths?.length??0,"asset card")}`;case"create_project":return e.name?`Create project "${e.name}"`:"Create design-system project";case"report_validate":return"Report validate metrics";default:return"Design sync"}}
function c9p(e,t){switch(e){case"needs_design_login":if(t?.isNonInteractiveSession)return`DesignSync needs design-system authorization, but /design-login requires an interactive terminal and is not available in this environment. If this is claude.ai/code, ask the user to use Claude Design's "Send to Claude Code Web" (which seeds the project into the workspace) or to provide the project files directly.`;return"DesignSync needs design-system authorization. Run /design-login to authorize it with your claude.ai account \u2014 this works even when this session authenticates with an API key or a provider token.";case"design_refresh_failed":if(t?.isNonInteractiveSession)return"Could not refresh the design access token (transient error). Retry shortly; if the error persists, the stored credential needs re-authorization from an interactive Claude Code terminal (not available here).";return"Could not refresh the design access token (transient error). Retry shortly, or run /design-login to re-authorize.";case"expand_failed":if(t?.isNonInteractiveSession)return"Could not add design scopes to the token. Re-authentication is required from an interactive Claude Code terminal (not available in this environment).";return'Could not add design scopes to the token. Run /login, select "Claude account with subscription", and retry \u2014 or run /design-login to authorize design access separately.';case"wrong_provider":return"DesignSync is only available with claude.ai authentication. It is not supported through Bedrock, Vertex, or other third-party providers.";case"essential_traffic_only":return"DesignSync is unavailable while nonessential network traffic is restricted (CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC is set). Unset it to use /design-sync."}}
async function p9p(e){let t=J_o(),n=await Y_o();if(!n.ok&&n.reason==="needs_design_login"&&t&&!e?.isNonInteractiveSession&&e?.permissionMode!==void 0&&d9p.has(e.permissionMode)){let r=await JXa(e?.signal);if(r.ok)return{accessToken:r.accessToken,expanded:!1};throw new DesignSyncPreconditionError(`DesignSync needs design-system authorization. ${r.message}`)}if(!n.ok){let r=n.reason==="expand_failed"?"DesignSync needs a claude.ai login. ":"",o=n.detail?` (${n.detail})`:"";throw new DesignSyncPreconditionError(`${r}${c9p(n.reason,{isNonInteractiveSession:e?.isNonInteractiveSession})}${o}`)}return{accessToken:n.accessToken,expanded:n.expanded}}
function dx(e,t,n){if(e===void 0)throw Error(`${n} requires "${t}"`);return e}
async function cQa(e){return oDe.realpath(Wte.resolve(isTmuxControlMode(),e??"."))}
async function h9p(e,t){let n=fL(e.path);if(e.localPath===void 0){if(e.data===void 0)throw Error(`write_files: ${n} has neither data nor localPath`);return{path:n,data:e.data,encoding:e.encoding,mimeType:e.mimeType}}if(e.data!==void 0)throw Error(`write_files: ${n} has both data and localPath`);if(t===void 0)throw Error("write_files with localPath requires a plan finalized with localDir. Re-run finalize_plan with the bundle directory.");let r=(p)=>p.endsWith(Wte.sep)?p:p+Wte.sep,o=Wte.resolve(t),s=Wte.resolve(o,e.localPath);if(s!==o&&!s.startsWith(r(o)))throw Error("write_files: localPath must be inside the directory approved at finalize_plan.");let[i,a]=await Promise.all([oDe.realpath(s),oDe.realpath(o)]);if(i!==a&&!i.startsWith(r(a)))throw Error("write_files: localPath resolves outside the directory approved at finalize_plan.");let l=Q_o.constants.O_NOFOLLOW,c=await oDe.open(i,Q_o.constants.O_RDONLY|l),u;try{let p=await c.stat();if(!p.isFile())throw Error("write_files: localPath must be a regular file.");if(p.size>lQa)throw Error(`write_files: file at localPath exceeds the ${lQa} byte limit.`);u=await c.readFile()}finally{await c.close()}let d=Wte.extname(i).slice(1).toLowerCase();return f9p.has(d)?{path:n,data:u.toString("utf8"),mimeType:e.mimeType}:{path:n,data:u.toString("base64"),encoding:"base64",mimeType:e.mimeType}}
async function g9p(e,t,n){switch(e.method){case"list_projects":{let{items:r}=await LXa(t,{type:W_o},n);return{method:"list_projects",projects:r.filter((s)=>s.canEdit??s.isOwned??!1).map((s)=>({projectId:s.projectId,name:s.name,ownerDisplayName:s.ownerDisplayName,isOwned:s.isOwned,updatedAt:s.updatedAt}))}}case"get_project":{let r=dx(e.projectId,"projectId",e.method),o=await NXa(t,r,n);return{method:"get_project",projectId:o.projectId,name:o.name,type:o.type,ownerDisplayName:o.ownerDisplayName,isOwned:o.isOwned,canEdit:o.canEdit}}case"list_files":{let r=dx(e.projectId,"projectId",e.method);return{method:"list_files",paths:await FXa(t,r,n)}}case"get_file":{let r=dx(e.projectId,"projectId",e.method),o=dx(e.path,"path",e.method),s=await BXa(t,r,o,void 0,n);return{method:"get_file",path:o,content:s.content,contentType:s.contentType,isBase64:s.isBase64,truncated:s.truncated}}case"finalize_plan":{let r=dx(e.projectId,"projectId",e.method),o=dx(e.writes,"writes",e.method).map(fL),s=dx(e.deletes,"deletes",e.method).map(fL),i=await cQa(e.localDir);return{method:"finalize_plan",planId:tQa({projectId:r,writes:o,deletes:s,localDir:i}),writes:o,deletes:s}}case"write_files":{let r=dx(e.projectId,"projectId",e.method),o=dx(e.planId,"planId",e.method),s=dx(e.files,"files",e.method),i=Kqt(o);if(!i||i.projectId!==r)throw Error("Plan token is missing or does not match this project. Call finalize_plan first.");let a=s.map((p)=>p.path).filter(K_o);if(a.length>0)throw Error(`Cannot write reserved paths: ${a.join(", ")}. CLAUDE.md and .claude/ carry instructions to the design agent and are blocked regardless of the plan.`);let l=s.map((p)=>fL(p.path)).filter((p)=>!Vqt(p,i.writes));if(l.length>0)throw Error(`Cannot write paths outside the finalized plan: ${l.join(", ")}. Re-run finalize_plan with the full set.`);let c=32,u=[];for(let p=0;p<s.length;p+=c){if(n.aborted)throw new $c;let m=s.slice(p,p+c);u.push(...await Promise.all(m.map((f)=>h9p(f,i.localDir))))}return{method:"write_files",written:(await MXa(t,r,u,{},n)).length}}case"delete_files":{let r=dx(e.projectId,"projectId",e.method),o=dx(e.planId,"planId",e.method),s=dx(e.paths,"paths",e.method),i=Kqt(o);if(!i||i.projectId!==r)throw Error("Plan token is missing or does not match this project. Call finalize_plan first.");let a=s.filter(K_o);if(a.length>0)throw Error(`Cannot delete reserved paths: ${a.join(", ")}. CLAUDE.md and .claude/ carry instructions to the design agent and are blocked regardless of the plan.`);let l=s.map(fL).filter((u)=>!Vqt(u,i.deletes));if(l.length>0)throw Error(`Cannot delete paths outside the finalized plan: ${l.join(", ")}. Re-run finalize_plan with the full set.`);return{method:"delete_files",deleted:await UXa(t,r,s.map(fL),n)}}case"register_assets":{let r=dx(e.projectId,"projectId",e.method),o=dx(e.planId,"planId",e.method),s=dx(e.assets,"assets",e.method),i=Kqt(o);if(!i||i.projectId!==r)throw Error("Plan token is missing or does not match this project. Call finalize_plan first.");let a=s.map((c)=>fL(c.path)).filter((c)=>!Vqt(c,i.writes));if(a.length>0)throw Error(`Cannot register paths outside the finalized plan: ${a.join(", ")}. Re-run finalize_plan with the full set.`);let l=0;for(let c of s){if(n.aborted)throw new $c;await qXa(t,r,{...c,path:fL(c.path)},n),l++}return{method:"register_assets",registered:l}}case"unregister_assets":{let r=dx(e.projectId,"projectId",e.method),o=dx(e.planId,"planId",e.method),s=dx(e.paths,"paths",e.method),i=Kqt(o);if(!i||i.projectId!==r)throw Error("Plan token is missing or does not match this project. Call finalize_plan first.");let a=s.map(fL).filter((c)=>!Vqt(c,i.deletes));if(a.length>0)throw Error(`Cannot unregister cards for paths outside the finalized plan's deletes: ${a.join(", ")}. Re-run finalize_plan with the full set.`);let l=0;for(let c of s.map(fL)){if(n.aborted)throw new $c;await WXa(t,r,c,n),l++}return{method:"unregister_assets",unregistered:l}}case"create_project":{let r=dx(e.name,"name",e.method),o=await $Xa(t,r,n);return{method:"create_project",projectId:o.projectId,name:o.name}}case"report_validate":return{method:"report_validate"}}}
var Q_o,oDe,Wte,n9p,r9p,o9p,s9p,qte,a9p,u9p="Upgraded your claude.ai login to include design-system access (user:design:read, user:design:write). This lets /design-sync read and write your org's design-system projects on claude.ai/design.",DesignSyncPreconditionError,d9p,DesignSyncTool,f9p,lQa=5242880;
var dQa=b(()=>{Qr();ri();xl();Po();Ct();tn();lr();KXa();y5n();T5n();nQa();iQa();Q_o=require("fs"),oDe=require("fs/promises"),Wte=require("path"),n9p=ve(()=>C.strictObject({path:C.string().min(1).max(F5e).describe("Path within the project, e.g. components/button/index.html"),localPath:C.string().min(1).optional().describe("Path on disk to read file contents from, relative to the localDir approved at finalize_plan. Preferred for anything you have on disk: the tool reads, encodes, and uploads directly so the contents never enter the model context. Mutually exclusive with data."),data:C.string().optional().describe("Inline file contents (UTF-8 text, or base64 when encoding is "+'"base64"). For small dynamic content only \u2014 anything you have on '+"disk should use localPath instead."),encoding:C.enum(["base64"]).optional().describe('Set to "base64" for binary inline data'),mimeType:C.string().optional()})),r9p=ve(()=>C.strictObject({name:C.string().min(1).max(255).describe('Short human-readable label ("Primary buttons"), not a path'),path:C.string().min(1).max(F5e).describe("Project-relative path to the preview/spec file this card renders"),subtitle:C.string().max(255).optional().describe('Variants shown ("Primary / secondary / ghost, 3 sizes")'),viewport:C.strictObject({width:C.number().int().positive(),height:C.number().int().positive().optional()}).optional().describe("Card dimensions in the Design System pane"),group:C.string().max(64).optional().describe("Free-form section label for the Design System pane (max 64 chars). "+"Use the source design system's own categorization if it has one \u2014 "+'e.g. Material has Buttons/Cards/Forms/etc., a corporate kit might have Actions/Forms/Navigation. Common foundational labels: "Type", "Colors", "Spacing", "Components", "Brand". The pane groups by the value you send.')})),o9p=ve(()=>C.strictObject({method:C.enum(["list_projects","get_project","list_files","get_file","finalize_plan","write_files","delete_files","register_assets","unregister_assets","create_project","report_validate"]),projectId:C.string().min(1).optional().describe("Required for all methods except list_projects and create_project"),path:C.string().min(1).optional().describe("get_file: file path to read"),writes:C.array(C.string().min(1).max(F5e)).max(256).optional().describe("finalize_plan: exact paths or glob patterns that will be written. `*` matches within a single segment, `**` matches any depth (e.g. `ui_kits/acme/**/*.html`). Max 3 `*`/`**` wildcards per "+"pattern and max 256 entries \u2014 use broader globs to cover more "+"files rather than enumerating paths."),deletes:C.array(C.string().min(1).max(F5e)).max(256).optional().describe("finalize_plan: exact paths or glob patterns that will be deleted (same syntax and limits as writes)."),planId:C.string().min(1).optional().describe("write_files/delete_files/register_assets/unregister_assets: token from a prior finalize_plan call"),files:C.array(n9p()).max(256).optional().describe("write_files: file contents to write (max 256 per call \u2014 split "+"larger bundles across multiple write_files calls under the same planId)."),paths:C.array(C.string().min(1).max(F5e)).max(256).optional().describe("delete_files: paths to delete. unregister_assets: paths whose "+"Design System pane card should be removed. Max 256 per call \u2014 "+"split larger batches across multiple calls under the same planId."),name:C.string().min(1).max(200).optional().describe("create_project: name for the new design-system project"),assets:C.array(r9p()).max(256).optional().describe("register_assets: cards to register in the Design System pane. Each path must be in the finalized plan. Run after write_files succeeds. Max 256 per call."),localDir:C.string().min(1).optional().describe("finalize_plan: directory the bundle was built into. write_files with localPath may only read files inside this directory. Defaults to the current working directory. Resolved to an absolute path and shown in the permission prompt."),counts:C.object({total:C.number().int().nonnegative(),bad:C.number().int().nonnegative(),thin:C.number().int().nonnegative(),variantsIdentical:C.number().int().nonnegative(),iterations:C.number().int().nonnegative()}).optional().describe("report_validate: aggregate from the final .render-check.json \u2014 "+"counts only, no component names or paths.")})),s9p={list_projects:{present:[],nonEmpty:[]},get_project:{present:["projectId"],nonEmpty:[]},list_files:{present:["projectId"],nonEmpty:[]},get_file:{present:["projectId","path"],nonEmpty:[]},finalize_plan:{present:["projectId","writes","deletes"],nonEmpty:[]},write_files:{present:["projectId","planId"],nonEmpty:["files"]},delete_files:{present:["projectId","planId"],nonEmpty:["paths"]},register_assets:{present:["projectId","planId"],nonEmpty:["assets"]},unregister_assets:{present:["projectId","planId"],nonEmpty:["paths"]},create_project:{present:["name"],nonEmpty:[]},report_validate:{present:["counts"],nonEmpty:[]}};qte={notice:C.string().optional()},a9p=ve(()=>C.discriminatedUnion("method",[C.object({method:C.literal("list_projects"),...qte,projects:C.array(C.object({projectId:C.string(),name:C.string(),ownerDisplayName:C.string().optional(),isOwned:C.boolean().optional(),updatedAt:C.string().optional()}))}),C.object({method:C.literal("get_project"),...qte,projectId:C.string(),name:C.string(),type:C.string().optional(),ownerDisplayName:C.string().optional(),isOwned:C.boolean().optional(),canEdit:C.boolean().optional()}),C.object({method:C.literal("list_files"),...qte,paths:C.array(C.string())}),C.object({method:C.literal("get_file"),...qte,path:C.string(),content:C.string(),contentType:C.string(),isBase64:C.boolean(),truncated:C.boolean()}),C.object({method:C.literal("finalize_plan"),...qte,planId:C.string(),writes:C.array(C.string()),deletes:C.array(C.string())}),C.object({method:C.literal("write_files"),...qte,written:C.number()}),C.object({method:C.literal("delete_files"),...qte,deleted:C.number()}),C.object({method:C.literal("register_assets"),...qte,registered:C.number()}),C.object({method:C.literal("unregister_assets"),...qte,unregistered:C.number()}),C.object({method:C.literal("create_project"),...qte,projectId:C.string(),name:C.string()}),C.object({method:C.literal("report_validate"),...qte})]));DesignSyncPreconditionError=class DesignSyncPreconditionError extends Error{constructor(e){super(e);this.name="DesignSyncPreconditionError"}};d9p=new Set(["default","acceptEdits","auto"]);DesignSyncTool=Ks({name:vBt,searchHint:"sync local design system components to a claude.ai/design project",shouldDefer:!0,maxResultSizeChars:300000,isEnabled(){return pmt()},async description(){return cto},async prompt(){return cto},get inputSchema(){return o9p()},get outputSchema(){return a9p()},isConcurrencySafe(){return!1},isReadOnly(e){return l9p(e.method)},isDestructive(e){return e.method==="write_files"||e.method==="delete_files"||e.method==="unregister_assets"},userFacingName(e){return`Design: ${X_o(e)}`},getToolUseSummary(e){return e?.method?X_o(e):null},toAutoClassifierInput(e){if(e.method==="finalize_plan"){let t=(n)=>{let r=n??[],o=50;if(r.length<=50)return r.join(", ");return`${r.length} paths (too many to list here; the user's permission prompt shows the full list)`};return`project ${e.projectId??"?"} from ${Wte.resolve(isTmuxControlMode(),e.localDir??".")}: write ${t(e.writes)}; delete ${t(e.deletes)}`}if(e.method==="create_project")return`create project "${e.name??"?"}"`;return e.method},renderToolUseMessage(e){if(e.method==="finalize_plan")return aQa(e.projectId);return X_o(e)},async validateInput(e){let t=i9p(e);if(t.length>0)return{result:!1,message:`${e.method} requires: ${t.join(", ")}.`,errorCode:1};if(e.method==="finalize_plan"&&(e.writes?.length??0)===0&&(e.deletes?.length??0)===0)return{result:!1,message:"finalize_plan needs at least one write or delete path.",errorCode:1};if(e.method==="write_files")for(let n of e.files??[]){let r=n.data!==void 0,o=n.localPath!==void 0;if(r===o)return{result:!1,message:`Each file needs exactly one of "data" or "localPath" (offending path: ${n.path}).`,errorCode:1};if(o&&n.encoding!==void 0)return{result:!1,message:`"encoding" only applies to inline "data"; localPath files are encoded automatically (offending path: ${n.path}).`,errorCode:1}}return{result:!0}},async checkPermissions(e){let t=sQa()?"DesignSync needs design-system access added to your claude.ai login (user:design:read, user:design:write). Approving refreshes "+"your token with these scopes \u2014 you'll be able to read and write "+"your org's design-system projects on claude.ai/design.":null,n=!t&&J_o()&&Gqt()&&!V_o()?"DesignSync needs design-system authorization for your claude.ai account. Approving opens your browser to authorize read and write access to your org's design-system projects "+"(user:design:read, user:design:write) \u2014 this session's own "+"authentication is not changed.":null,r=t??n;if(r&&e.method!=="finalize_plan"&&e.method!=="create_project"&&e.method!=="report_validate")return{behavior:"ask",message:r,updatedInput:e,decisionReason:{type:"safetyCheck",reason:t?"scope expansion \u2014 approving persists user:design:write to the OAuth credential store":"design login \u2014 approving opens a browser OAuth consent and stores a design credential",classifierApprovable:!1}};if(e.method==="finalize_plan"){let o=(e.writes??[]).map(fL),s=(e.deletes??[]).map(fL),i;try{i=await cQa(e.localDir)}catch(_){return{behavior:"deny",message:`localDir does not exist or is not accessible: ${e.localDir??isTmuxControlMode()} (${Ce(_)})`,decisionReason:{type:"safetyCheck",reason:"localDir not found",classifierApprovable:!1}}}let a=o.filter(N5e),l=o.filter((_)=>!N5e(_)),c=s.filter(N5e),u=s.filter((_)=>!N5e(_)),d=await Promise.all(l.map(async(_)=>{try{return await oDe.stat(Wte.resolve(i,_)),!0}catch{return!1}})),p=l.filter((_,T)=>!d[T]),h=l.length-p.length>0&&p.length>0?`\u26A0 ${p.length} of ${l.length} literal write ${Sn(l.length,"path")} not found under localDir \u2014 `+`expected if they use a different localPath or inline data, otherwise check for a typo: ${p.slice(0,5).join(", ")}`+(p.length>5?`, \u2026 and ${p.length-5} more`:""):null;return{behavior:"ask",message:[r,`To project: ${aQa(e.projectId)}`,`From folder: ${i}`,l.length>0?`Upload ${l.length} ${Sn(l.length,"file")}: ${l.join(", ")}`:null,a.length>0?`Upload files matching: ${a.join(", ")}`:null,h,u.length>0?`Delete ${u.length} ${Sn(u.length,"file")}: ${u.join(", ")}`:null,c.length>0?`Delete files matching: ${c.join(", ")}`:null].filter((_)=>_!==null).join(`
`),updatedInput:{...e,localDir:i},decisionReason:{type:"safetyCheck",reason:r?"Approving also grants Claude ongoing write access to your design projects.":"Review what will be uploaded before continuing.",classifierApprovable:!1}}}if(e.method==="create_project")return{behavior:"ask",message:[r,`Create design-system project "${e.name??"?"}" on claude.ai/design. The new project will be visible to your whole org (server default \u2014 you can change this from the Share menu after creation).`].filter((o)=>o!==null).join(`
`),updatedInput:e,decisionReason:{type:"safetyCheck",reason:r?"Approving also grants Claude ongoing write access to your design projects.":"This creates a new project on your claude.ai account.",classifierApprovable:!1}};return{behavior:"allow",updatedInput:e}},async call(e,t){let n=t.abortController.signal;if(e.method==="report_validate")return{data:{method:"report_validate"}};let r="";try{let o=await p9p({signal:n,isNonInteractiveSession:t.options?.isNonInteractiveSession,permissionMode:Mr(t).mode});r=o.accessToken;let s=await g9p(e,r,n);return{data:o.expanded?{...s,notice:u9p}:s}}catch(o){if(n.aborted)throw new $c;let s=GXa(Ce(o),r);if(o instanceof DesignSyncPreconditionError)throw new DesignSyncPreconditionError(s);throw Error(s)}},mapToolResultToToolResultBlockParam(e,t){return{tool_use_id:t,type:"tool_result",content:TeamDeleteToolName(e)}}});f9p=new Set(["html","css","js","jsx","mjs","cjs","ts","tsx","mts","cts","json","svg","xml","md","txt","csv","yaml","yml","toml"])});
export {uQa,i9p,l9p,aQa,X_o,c9p,p9p,dx,cQa,h9p,g9p,Q_o,oDe,Wte,n9p,r9p,o9p,s9p,qte,a9p,u9p,DesignSyncPreconditionError,d9p,DesignSyncTool,f9p,lQa,dQa};
