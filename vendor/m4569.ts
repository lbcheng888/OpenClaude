// @ts-nocheck
import {or,dn} from "../src/config/0137_namespace.ts";
import {Js,rT} from "./m1294.ts";
import {qt,TeamDeleteToolName,tn} from "../src/config/0230_encoding.ts";
import {logForDebugging,qe} from "../src/config/0236_setHasFormattedOutput.ts";
import {Ce,Ct} from "./m197.ts";
import {b} from "../runtime.ts";
async function nhl(e){while(gKn)await gKn;let t;gKn=new Promise((n)=>{t=n});try{return await e()}finally{gKn=null,t?.()}}
function rhl(){return thl.join(or(),LQp)}
function mRo(){return{version:X8e,lastComputedDate:null,dailyActivity:[],dailyModelTokens:[],modelUsage:{},totalSessions:0,totalMessages:0,longestSession:null,firstSessionDate:null,hourCounts:{},totalSpeculationTimeSavedMs:0,shotDistribution:{}}}
function MQp(e){if(typeof e.version!=="number"||e.version<OQp||e.version>X8e)return null;if(!Array.isArray(e.dailyActivity)||!Array.isArray(e.dailyModelTokens)||typeof e.totalSessions!=="number"||typeof e.totalMessages!=="number")return null;return{version:X8e,lastComputedDate:e.lastComputedDate??null,dailyActivity:e.dailyActivity,dailyModelTokens:e.dailyModelTokens,modelUsage:e.modelUsage??{},totalSessions:e.totalSessions,totalMessages:e.totalMessages,longestSession:e.longestSession??null,firstSessionDate:e.firstSessionDate??null,hourCounts:e.hourCounts??{},totalSpeculationTimeSavedMs:e.totalSpeculationTimeSavedMs??0,shotDistribution:e.shotDistribution}}
async function ohl(){let e=rhl();try{let t=await Js().read(e),n=qt(t);if(n.version!==X8e){let r=MQp(n);if(!r)return logForDebugging(`Stats cache version ${n.version} not migratable (expected ${X8e}), returning empty cache`),mRo();return logForDebugging(`Migrated stats cache from v${n.version} to v${X8e}`),await K8t(r),r}if(!Array.isArray(n.dailyActivity)||!Array.isArray(n.dailyModelTokens)||typeof n.totalSessions!=="number"||typeof n.totalMessages!=="number")return logForDebugging("Stats cache has invalid structure, returning empty cache"),mRo();return n}catch(t){return logForDebugging(`Failed to load stats cache: ${Ce(t)}`),mRo()}}
async function K8t(e){let t=rhl();try{let n=or();await Js().mkdir(n);let r=TeamDeleteToolName(e,null,2);await Js().atomicWrite(t,r,384),logForDebugging(`Stats cache saved successfully (lastComputedDate: ${e.lastComputedDate})`)}catch(n){logForDebugging(`Failed to save stats cache: ${Ce(n)}`,{level:"error"})}}
function fRo(e,t,n){let r=new Map;for(let p of e.dailyActivity)r.set(p.date,{...p});for(let p of t.dailyActivity){let m=r.get(p.date);if(m)m.messageCount+=p.messageCount,m.sessionCount+=p.sessionCount,m.toolCallCount+=p.toolCallCount;else r.set(p.date,{...p})}let o=new Map;for(let p of e.dailyModelTokens)o.set(p.date,{...p.tokensByModel});for(let p of t.dailyModelTokens){let m=o.get(p.date);if(m)for(let[f,h]of Object.entries(p.tokensByModel))m[f]=(m[f]||0)+h;else o.set(p.date,{...p.tokensByModel})}let s={...e.modelUsage};for(let[p,m]of Object.entries(t.modelUsage))if(s[p])s[p]={inputTokens:s[p].inputTokens+m.inputTokens,outputTokens:s[p].outputTokens+m.outputTokens,cacheReadInputTokens:s[p].cacheReadInputTokens+m.cacheReadInputTokens,cacheCreationInputTokens:s[p].cacheCreationInputTokens+m.cacheCreationInputTokens,webSearchRequests:s[p].webSearchRequests+m.webSearchRequests,costUSD:s[p].costUSD+m.costUSD,contextWindow:Math.max(s[p].contextWindow,m.contextWindow),maxOutputTokens:Math.max(s[p].maxOutputTokens,m.maxOutputTokens)};else s[p]={...m};let i={...e.hourCounts};for(let[p,m]of Object.entries(t.hourCounts)){let f=parseInt(p,10);i[f]=(i[f]||0)+m}let a=e.totalSessions+t.sessionStats.length,l=e.totalMessages+t.totalMessages,c=e.longestSession;for(let p of t.sessionStats)if(!c||p.duration>c.duration)c=p;let u=e.firstSessionDate;for(let p of t.sessionStats)if(!u||p.timestamp<u)u=p.timestamp;return{version:X8e,lastComputedDate:n,dailyActivity:Array.from(r.values()).sort((p,m)=>p.date.localeCompare(m.date)),dailyModelTokens:Array.from(o.entries()).map(([p,m])=>({date:p,tokensByModel:m})).sort((p,m)=>p.date.localeCompare(m.date)),modelUsage:s,totalSessions:a,totalMessages:l,longestSession:c,firstSessionDate:u,hourCounts:i,totalSpeculationTimeSavedMs:e.totalSpeculationTimeSavedMs+t.totalSpeculationTimeSavedMs}}
function cne(e){let n=e.toISOString().split("T")[0];if(!n)throw Error("Invalid ISO date string");return n}
function hRo(){return cne(new Date)}
function shl(){let e=new Date;return e.setDate(e.getDate()-1),cne(e)}
function Q8e(e,t){return e<t}
var thl,X8e=4,OQp=1,LQp="stats-cache.json",gKn=null;
var gRo=b(()=>{rT();qe();dn();Ct();tn();thl=require("path")});
export {nhl,rhl,mRo,MQp,ohl,K8t,fRo,cne,hRo,shl,Q8e,thl,X8e,OQp,LQp,gKn,gRo};
