// @ts-nocheck
import {Sf,f6t,Y5e,tTo,mx,Imt} from "./m4337.ts";
import {oq,sq,NPn,Yae} from "../src/config/3274_errors.ts";
import {e6,QD,Vte,z5e} from "./m4334.ts";
import {b} from "../runtime.ts";
function rTo(e){let t=Sf(e);return O4p.has(t)}
function M4p(e){return e.length>=3&&"-itemtype".startsWith(e)||e.length>=3&&"-type".startsWith(e)}
function oTo(e){if(Sf(e.name)!=="new-item")return!1;for(let n=0;n<e.args.length;n++){let r=e.args[n]??"";if(r.length===0)continue;let s=(oq.has(r[0])||r[0]==="/"?"-"+r.slice(1):r).toLowerCase(),i=s.indexOf(":",1),a=i>0?s.slice(0,i):s,l=e6(a.replace(/`[\r\n]+\s*/g,"")).toLowerCase();if(!M4p(l))continue;let c=i>0?s.slice(i+1):e.args[n+1]?.toLowerCase()??"",u=QD(Vte(e6(c.replace(/`[\r\n]+\s*/g,"")))).toLowerCase();if(/[?*[\]($]/.test(u))return!0;for(let d of L4p)if(u.length>0&&d.startsWith(u))return!0}return!1}
function sTo(e,t,n){if(n.mode==="bypassPermissions"||n.mode==="dontAsk")return{behavior:"passthrough",message:"Mode is handled in main permission flow"};if(n.mode!=="acceptEdits")return{behavior:"passthrough",message:"No mode-specific validation required"};if(!t.valid)return{behavior:"passthrough",message:"Cannot validate mode for unparsed command"};let r=sq(t);if(r.hasSubExpressions||r.hasScriptBlocks||r.hasMemberInvocations||r.hasSplatting||r.hasAssignments||r.hasStopParsing||r.hasExpandableStrings)return{behavior:"passthrough",message:"Command contains subexpressions, script blocks, or member invocations that require approval"};let o=NPn(t);if(o.length===0)return{behavior:"passthrough",message:"No commands found to validate for acceptEdits mode"};let s=o.reduce((a,l)=>a+l.commands.length,0);if(o.some((a)=>a.commands.some((l)=>l.elementType==="CommandAst"&&oTo(l))))return{behavior:"passthrough",message:"Command creates a filesystem link (New-Item -ItemType SymbolicLink/Junction/HardLink) \u2014 cannot auto-allow because later path validation cannot follow just-created links"};if(s>1){let a=!1,l=!1;for(let c of o)for(let u of c.commands){if(u.elementType!=="CommandAst")continue;if(f6t(u.name))a=!0;if(rTo(u.name))l=!0}if(a&&l)return{behavior:"passthrough",message:"Compound command contains a directory-changing command (Set-Location/Push-Location/Pop-Location) with a write operation \u2014 cannot auto-allow because path validation uses stale cwd"}}for(let a of o){for(let l of a.commands){if(l.elementType!=="CommandAst")return{behavior:"passthrough",message:`Pipeline contains expression source (${l.elementType}) that cannot be statically validated`};if(l.nameType==="application")return{behavior:"passthrough",message:`Command '${l.name}' resolved from a path-like name and requires approval`};if(l.elementTypes)for(let c=1;c<l.elementTypes.length;c++){let u=l.elementTypes[c];if(u!=="StringConstant"&&u!=="Parameter")return{behavior:"passthrough",message:`Command argument has unvalidatable type (${u}) \u2014 variable paths cannot be statically resolved`};if(u==="Parameter"){let d=l.args[c-1]??"",p=d.indexOf(":");if(p>0&&/[$(@{[]/.test(d.slice(p+1)))return{behavior:"passthrough",message:"Colon-bound parameter contains an expression that cannot be statically validated"}}}if(Y5e(l.name)||tTo(l,e.command))continue;if(!rTo(l.name))return{behavior:"passthrough",message:`No mode-specific handling for '${l.name}' in acceptEdits mode`};if(mx(l.name,l))return{behavior:"passthrough",message:`Arguments in '${l.name}' cannot be statically validated in acceptEdits mode`}}if(a.nestedCommands)for(let l of a.nestedCommands){if(l.elementType!=="CommandAst")return{behavior:"passthrough",message:`Nested expression element (${l.elementType}) cannot be statically validated`};if(l.nameType==="application")return{behavior:"passthrough",message:`Nested command '${l.name}' resolved from a path-like name and requires approval`};if(Y5e(l.name)||tTo(l,e.command))continue;if(!rTo(l.name))return{behavior:"passthrough",message:`No mode-specific handling for '${l.name}' in acceptEdits mode`};if(mx(l.name,l))return{behavior:"passthrough",message:`Arguments in nested '${l.name}' cannot be statically validated in acceptEdits mode`}}}return{behavior:"allow",updatedInput:e,decisionReason:{type:"mode",mode:"acceptEdits"}}}
var O4p,L4p;
var zel=b(()=>{Yae();Imt();z5e();O4p=new Set(["set-content","add-content","remove-item","clear-content"]);L4p=new Set(["symboliclink","junction","hardlink"])});
export {rTo,M4p,oTo,sTo,O4p,L4p,zel};
