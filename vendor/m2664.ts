// @ts-nocheck
import {b} from "../runtime.ts";
import {iv} from "./m454.ts";
import {hn} from "./m251.ts";
var mMi,jqr,cvn,pMi,BSd,FSd,fMi,AMi,hMi,gMi,_Mi,USd,SandboxRuntimeConfigSchema;
var Gqr=b(()=>{iv();mMi=require("path"),jqr=hn.string().refine((e)=>{if(e.includes("://")||e.includes("/")||e.includes(":"))return!1;if(e==="localhost")return!0;if(e.startsWith("*.")){let t=e.slice(2);if(!t.includes(".")||t.startsWith(".")||t.endsWith("."))return!1;let n=t.split(".");return n.length>=2&&n.every((r)=>r.length>0)}if(e.includes("*"))return!1;return e.includes(".")&&!e.startsWith(".")&&!e.endsWith(".")},{message:'Invalid domain pattern. Must be a valid domain (e.g., "example.com") or wildcard (e.g., "*.example.com"). Overly broad patterns like "*.com" or "*" are not allowed for security reasons.'}),cvn=hn.string().min(1,"Path cannot be empty"),pMi=hn.string().min(1,"Path cannot be empty").refine((e)=>mMi.isAbsolute(e),{message:"Binary path must be absolute"}),BSd=hn.object({socketPath:hn.string().min(1).describe("Unix socket path to the MITM proxy"),domains:hn.array(jqr).min(1).describe('Domains to route through the MITM proxy (e.g., ["api.example.com", "*.internal.org"])')}),FSd=hn.object({http:hn.string().url().optional().describe("Upstream proxy URL for plain HTTP traffic"),https:hn.string().url().optional().describe("Upstream proxy URL for HTTPS/CONNECT traffic (falls back to http if unset)"),noProxy:hn.string().optional().describe("Comma-separated NO_PROXY list (hostname suffixes and CIDR ranges). Matching destinations connect directly instead of via the parent proxy.")}),fMi=hn.object({allowedDomains:hn.array(jqr).describe('List of allowed domains (e.g., ["github.com", "*.npmjs.org"])'),deniedDomains:hn.array(jqr).describe("List of denied domains"),allowUnixSockets:hn.array(hn.string()).optional().describe("macOS only: Unix socket paths to allow. Ignored on Linux (seccomp cannot filter by path)."),allowAllUnixSockets:hn.boolean().optional().describe("If true, allow all Unix sockets (disables blocking on both platforms)."),allowLocalBinding:hn.boolean().optional().describe("Whether to allow binding to local ports (default: false)"),allowMachLookup:hn.array(hn.string().refine((e)=>!(e.endsWith("*")?e.slice(0,-1):e).includes("*"),{message:'Wildcards are only allowed as a single trailing "*" (e.g., "com.example.*" or "*" for all services).'})).optional().describe('macOS only: Additional XPC/Mach service names to allow looking up. Supports trailing-wildcard prefix matching (e.g., "2BUA8C4S2C.com.1password.*"). Needed for tools like 1Password CLI, Playwright, or the iOS Simulator that communicate via XPC.'),httpProxyPort:hn.number().int().min(1).max(65535).optional().describe("Port of an external HTTP proxy to use instead of starting a local one. When provided, the library will skip starting its own HTTP proxy and use this port. The external proxy must handle domain filtering."),socksProxyPort:hn.number().int().min(1).max(65535).optional().describe("Port of an external SOCKS proxy to use instead of starting a local one. When provided, the library will skip starting its own SOCKS proxy and use this port. The external proxy must handle domain filtering."),mitmProxy:BSd.optional().describe("Optional MITM proxy configuration. Routes matching domains through an upstream proxy via Unix socket while SRT still handles allow/deny filtering."),filterRequest:hn.custom((e)=>typeof e==="function",{message:"filterRequest must be a function"}).optional().describe("Per-request filter callback. Receives the parsed HTTP request (web-standard Request) and returns {action, reason?}. Denied requests get a 403 with the reason. If the callback throws, the request is denied. Applies to plain HTTP through the proxy and, when tlsTerminate is configured, to terminated HTTPS. SRT does not provide a policy language; library consumers own matching."),tlsTerminate:hn.object({caCertPath:hn.string().min(1).optional().describe("Path to a PEM-encoded CA certificate. The sandboxed child is configured to trust this CA, and the TLS-terminating proxy uses it to sign per-host certificates. If omitted, SRT generates an ephemeral CA into a temp directory for the lifetime of the session."),caKeyPath:hn.string().min(1).optional().describe("Path to the PEM-encoded private key for caCertPath.")}).refine((e)=>!e.caCertPath===!e.caKeyPath,{message:"caCertPath and caKeyPath must be provided together"}).optional().describe("[EXPERIMENTAL] Enable in-process TLS termination so HTTPS request/response bodies are visible to SRT. Provide a CA cert+key, or omit both to have SRT generate an ephemeral one."),parentProxy:FSd.optional().describe("Upstream HTTP proxy for outbound connections. When set, SRT's proxy tunnels non-mitmProxy traffic through this parent instead of connecting directly. Falls back to HTTP_PROXY/HTTPS_PROXY/NO_PROXY env vars if unset.")}),AMi=hn.object({denyRead:hn.array(cvn).describe("Paths denied for reading"),allowRead:hn.array(cvn).optional().describe("Paths to re-allow reading within denied regions (takes precedence over denyRead). Use with denyRead to deny a broad region then allow back specific subdirectories."),allowWrite:hn.array(cvn).describe("Paths allowed for writing"),denyWrite:hn.array(cvn).describe("Paths denied for writing (takes precedence over allowWrite)"),allowGitConfig:hn.boolean().optional().describe("Allow writes to .git/config files (default: false). Enables git remote URL updates while keeping .git/hooks protected.")}),hMi=hn.record(hn.string(),hn.array(hn.string())).describe('Map of command patterns to filesystem paths to ignore violations for. Use "*" to match all commands'),gMi=hn.object({command:hn.string().describe("The ripgrep command to execute"),args:hn.array(hn.string()).optional().describe("Additional arguments to pass before ripgrep args"),argv0:hn.string().optional().describe("Override argv[0] when spawning (for multicall binaries that dispatch on argv[0])")}),_Mi=hn.object({groupName:hn.string().min(1).default("sandbox-runtime-net").describe("Discriminator group name. Must match the group created at install time. Ignored if groupSid is set."),groupSid:hn.string().regex(/^S-1-/,"must be an S-1-\u2026 SID string").optional().describe("Discriminator group SID. Overrides groupName lookup \u2014 use for "+"domain groups or where name resolution is unreliable."),wfpSublayerGuid:hn.string().uuid().optional().describe("WFP sublayer GUID under which the filters were installed. Omit to use the srt-win compile-time default. Set this when filters were installed by enterprise tooling under a custom sublayer."),proxyPortRange:hn.tuple([hn.number().int().min(1),hn.number().int().max(65535)]).refine(([e,t])=>e<=t&&t-e<=64,{message:"low must be \u2264 high and range width \u2264 64"}).optional().describe("Inclusive [low, high] port range the JS http/socks proxies bind inside. MUST match the range passed to `srt-win wfp install "+"--proxy-port-range` (default 60080\u201360089) \u2014 the WFP loopback "+"permit only covers ports in that range.")}),USd=hn.object({applyPath:hn.string().optional().describe("Path to the apply-seccomp binary"),argv0:hn.string().optional().describe("Invoke apply-seccomp as a multicall binary that dispatches on the ARGV0 environment variable. When set, applyPath is used verbatim (no existence check) and the invocation inside bwrap is prefixed with ARGV0=<this value>. The caller is responsible for ensuring applyPath resolves inside the bwrap namespace and that the target binary implements the apply-seccomp interface when ARGV0 matches.")}),SandboxRuntimeConfigSchema=hn.object({network:fMi.describe("Network restrictions configuration"),filesystem:AMi.describe("Filesystem restrictions configuration"),ignoreViolations:hMi.optional().describe("Optional configuration for ignoring specific violations"),enableWeakerNestedSandbox:hn.boolean().optional().describe("Enable weaker nested sandbox mode (for Docker environments)"),enableWeakerNetworkIsolation:hn.boolean().optional().describe("Enable weaker network isolation to allow access to com.apple.trustd.agent (macOS only). This is needed for Go programs (gh, gcloud, terraform, kubectl, etc.) to verify TLS certificates when using httpProxyPort with a MITM proxy and custom CA. Enabling this opens a potential data exfiltration vector through the trustd service. Only enable if you need Go TLS verification."),allowAppleEvents:hn.boolean().optional().describe("Allow sending Apple Events and Launch Services open requests from the sandbox (macOS only). Needed for open, osascript, and anything that opens URLs or scripts other apps via AppleScript. This removes code-execution isolation: sandboxed commands can launch other applications unsandboxed with no user prompt (launched apps are not subject to the sandbox filesystem or network restrictions), and can script running apps subject to TCC automation consent. Default: false."),ripgrep:gMi.optional().describe('Custom ripgrep configuration (default: { command: "rg" })'),mandatoryDenySearchDepth:hn.number().int().min(1).max(10).optional().describe("Maximum directory depth to search for dangerous files on Linux (default: 3). Higher values provide more protection but slower performance."),allowPty:hn.boolean().optional().describe("Allow pseudo-terminal (pty) operations (macOS only)"),seccomp:USd.optional().describe("Custom seccomp binary paths (Linux only)."),bwrapPath:pMi.optional().describe('Linux only: absolute path to the bwrap (bubblewrap) binary. When set, this path is used directly instead of resolving "bwrap" via PATH.'),socatPath:pMi.optional().describe('Linux only: absolute path to the socat binary. When set, this path is used directly instead of resolving "socat" via PATH.'),windows:_Mi.optional().describe("Windows-specific settings (group, WFP sublayer, proxy port range).")})});
export {mMi,jqr,cvn,pMi,BSd,FSd,fMi,AMi,hMi,gMi,_Mi,USd,SandboxRuntimeConfigSchema,Gqr};
