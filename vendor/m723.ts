// @ts-nocheck
import {E} from "./m319.ts";
import {ins,ans} from "./m722.ts";
import {b} from "../runtime.ts";
import {sQ} from "./m721.ts";
import {Xr} from "./m321.ts";
import {we} from "./m455.ts";
import {HOOK_EVENTS} from "./m718.ts";
function CXc(){let e=E.object({type:E.literal("command").describe("Shell command hook type"),command:E.string().describe("Shell command to execute"),args:E.array(E.string()).optional().describe("Argument list for exec form. When present, `command` is resolved as "+"an executable and spawned directly with these arguments \u2014 no shell. "+"Path placeholders like ${CLAUDE_PLUGIN_ROOT} are substituted per-element as plain strings, so paths with quotes, $, or backticks never reach a shell parser. When absent, `command` runs through a shell (bash on POSIX, PowerShell on Windows without Git Bash)."),if:_Et(),shell:E.enum(ins).optional().describe("Shell interpreter. 'bash' uses your $SHELL (bash/zsh/sh); 'powershell' uses pwsh. Defaults to bash (powershell on Windows without Git Bash)."),timeout:E.number().positive().optional().describe("Timeout in seconds for this specific command"),statusMessage:E.string().optional().describe("Custom status message to display in spinner while hook runs"),once:E.boolean().optional().describe("If true, hook runs once and is removed after execution"),async:E.boolean().optional().describe("If true, hook runs in background without blocking"),asyncRewake:E.boolean().optional().describe("If true, hook runs in background and wakes the model on exit code 2 (blocking error). Implies async."),rewakeMessage:E.string().min(1).optional().describe("@internal Custom prefix for the system-reminder shown to the model when an asyncRewake hook exits with code 2. The hook output is appended after this prefix."),rewakeSummary:E.string().min(1).optional().describe('@internal One-line summary shown to the user in the terminal when an asyncRewake hook exits with code 2. Defaults to "Stop hook feedback".')}),t=E.object({type:E.literal("prompt").describe("LLM prompt hook type"),prompt:E.string().describe("Prompt to evaluate with LLM. Use $ARGUMENTS placeholder for hook input JSON."),if:_Et(),timeout:E.number().positive().optional().describe("Timeout in seconds for this specific prompt evaluation"),model:E.string().optional().describe('Model to use for this prompt hook (e.g., "claude-sonnet-4-6"). If not specified, uses the default small fast model.'),continueOnBlock:E.boolean().optional().describe(`Sets the continue value for the decision:"block" produced when ok is false. Default false (turn ends). Whether continue:true lets the turn proceed depends on the event's decision:"block" semantics. On PostToolUse, the reason is fed back to Claude and the turn continues.`),statusMessage:E.string().optional().describe("Custom status message to display in spinner while hook runs"),once:E.boolean().optional().describe("If true, hook runs once and is removed after execution")}),n=E.object({type:E.literal("mcp_tool").describe("MCP tool hook type"),server:E.string().describe("Name of an already-configured MCP server to invoke"),tool:E.string().describe("Name of the tool on that server to call"),input:E.record(E.string(),E.unknown()).optional().describe('Arguments passed to the MCP tool. String values support ${path} interpolation from the hook input JSON (e.g. "${tool_input.file_path}").'),if:_Et(),timeout:E.number().positive().optional().describe("Timeout in seconds for this specific tool call"),statusMessage:E.string().optional().describe("Custom status message to display in spinner while hook runs"),once:E.boolean().optional().describe("If true, hook runs once and is removed after execution")}),r=E.object({type:E.literal("http").describe("HTTP hook type"),url:E.string().url().describe("URL to POST the hook input JSON to"),if:_Et(),timeout:E.number().positive().optional().describe("Timeout in seconds for this specific request"),headers:E.record(E.string(),E.string()).optional().describe('Additional headers to include in the request. Values may reference environment variables using $VAR_NAME or ${VAR_NAME} syntax (e.g., "Authorization": "Bearer $MY_TOKEN"). Only variables listed in allowedEnvVars will be interpolated.'),allowedEnvVars:E.array(E.string()).optional().describe("Explicit list of environment variable names that may be interpolated in header values. Only variables listed here will be resolved; all other $VAR references are left as empty strings. Required for env var interpolation to work."),statusMessage:E.string().optional().describe("Custom status message to display in spinner while hook runs"),once:E.boolean().optional().describe("If true, hook runs once and is removed after execution")}),o=E.object({type:E.literal("agent").describe("Agentic verifier hook type"),prompt:E.string().describe('Prompt describing what to verify (e.g. "Verify that unit tests ran and passed."). Use $ARGUMENTS placeholder for hook input JSON.'),if:_Et(),timeout:E.number().positive().optional().describe("Timeout in seconds for agent execution (default 60)"),model:E.string().optional().describe('Model to use for this agent hook (e.g., "claude-sonnet-4-6"). If not specified, uses Haiku.'),statusMessage:E.string().optional().describe("Custom status message to display in spinner while hook runs"),once:E.boolean().optional().describe("If true, hook runs once and is removed after execution")});return{BashCommandHookSchema:e,PromptHookSchema:t,HttpHookSchema:r,AgentHookSchema:o,McpToolHookSchema:n}}
var _Et,lns,cns,K3;
var dnn=b(()=>{sQ();Xr();ans();_Et=we(()=>E.string().optional().describe('Permission rule syntax to filter when this hook runs (e.g., "Bash(git *)"). Only runs if the tool call matches the pattern. Avoids spawning hooks for non-matching commands.'));lns=we(()=>{let{BashCommandHookSchema:e,PromptHookSchema:t,AgentHookSchema:n,HttpHookSchema:r,McpToolHookSchema:o}=CXc();return E.discriminatedUnion("type",[e,t,n,r,o])}),cns=we(()=>E.object({matcher:E.string().optional().describe('String pattern to match (e.g. tool names like "Write")'),hooks:E.array(lns()).describe("List of hooks to execute when the matcher matches")})),K3=we(()=>E.partialRecord(E.enum(HOOK_EVENTS),E.array(cns())))});
export {CXc,_Et,lns,cns,K3,dnn};
