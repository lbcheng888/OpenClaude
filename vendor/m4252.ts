// @ts-nocheck
import {b} from "../runtime.ts";
import {Xr} from "./m321.ts";
import {we} from "./m455.ts";
import {E} from "./m319.ts";
var IWa;
var DWa=b(()=>{Xr();IWa=we(()=>{let e=E.strictObject({operation:E.literal("goToDefinition"),filePath:E.string().describe("The absolute or relative path to the file"),line:E.number().int().positive().describe("The line number (1-based, as shown in editors)"),character:E.number().int().positive().describe("The character offset (1-based, as shown in editors)"),query:E.string().optional()}),t=E.strictObject({operation:E.literal("findReferences"),filePath:E.string().describe("The absolute or relative path to the file"),line:E.number().int().positive().describe("The line number (1-based, as shown in editors)"),character:E.number().int().positive().describe("The character offset (1-based, as shown in editors)"),query:E.string().optional()}),n=E.strictObject({operation:E.literal("hover"),filePath:E.string().describe("The absolute or relative path to the file"),line:E.number().int().positive().describe("The line number (1-based, as shown in editors)"),character:E.number().int().positive().describe("The character offset (1-based, as shown in editors)"),query:E.string().optional()}),r=E.strictObject({operation:E.literal("documentSymbol"),filePath:E.string().describe("The absolute or relative path to the file"),line:E.number().int().positive().describe("The line number (1-based, as shown in editors)"),character:E.number().int().positive().describe("The character offset (1-based, as shown in editors)"),query:E.string().optional()}),o=E.strictObject({operation:E.literal("workspaceSymbol"),filePath:E.string().describe("The absolute or relative path to the file"),line:E.number().int().positive().describe("The line number (1-based, as shown in editors)"),character:E.number().int().positive().describe("The character offset (1-based, as shown in editors)"),query:E.string().optional().describe("The symbol name or partial name to search for. Most language servers return no results for an empty query.")}),s=E.strictObject({operation:E.literal("goToImplementation"),filePath:E.string().describe("The absolute or relative path to the file"),line:E.number().int().positive().describe("The line number (1-based, as shown in editors)"),character:E.number().int().positive().describe("The character offset (1-based, as shown in editors)"),query:E.string().optional()}),i=E.strictObject({operation:E.literal("prepareCallHierarchy"),filePath:E.string().describe("The absolute or relative path to the file"),line:E.number().int().positive().describe("The line number (1-based, as shown in editors)"),character:E.number().int().positive().describe("The character offset (1-based, as shown in editors)"),query:E.string().optional()}),a=E.strictObject({operation:E.literal("incomingCalls"),filePath:E.string().describe("The absolute or relative path to the file"),line:E.number().int().positive().describe("The line number (1-based, as shown in editors)"),character:E.number().int().positive().describe("The character offset (1-based, as shown in editors)"),query:E.string().optional()}),l=E.strictObject({operation:E.literal("outgoingCalls"),filePath:E.string().describe("The absolute or relative path to the file"),line:E.number().int().positive().describe("The line number (1-based, as shown in editors)"),character:E.number().int().positive().describe("The character offset (1-based, as shown in editors)"),query:E.string().optional()});return E.discriminatedUnion("operation",[e,t,n,r,o,s,i,a,l])})});
export {IWa,DWa};
