// @ts-nocheck
import {b} from "../runtime.ts";
import {Qr} from "./m323.ts";
import {ve} from "./m461.ts";
import {C} from "./m321.ts";
var XYa;
var QYa=b(()=>{Qr();XYa=ve(()=>{let e=C.strictObject({operation:C.literal("goToDefinition"),filePath:C.string().describe("The absolute or relative path to the file"),line:C.number().int().positive().describe("The line number (1-based, as shown in editors)"),character:C.number().int().positive().describe("The character offset (1-based, as shown in editors)"),query:C.string().optional()}),t=C.strictObject({operation:C.literal("findReferences"),filePath:C.string().describe("The absolute or relative path to the file"),line:C.number().int().positive().describe("The line number (1-based, as shown in editors)"),character:C.number().int().positive().describe("The character offset (1-based, as shown in editors)"),query:C.string().optional()}),n=C.strictObject({operation:C.literal("hover"),filePath:C.string().describe("The absolute or relative path to the file"),line:C.number().int().positive().describe("The line number (1-based, as shown in editors)"),character:C.number().int().positive().describe("The character offset (1-based, as shown in editors)"),query:C.string().optional()}),r=C.strictObject({operation:C.literal("documentSymbol"),filePath:C.string().describe("The absolute or relative path to the file"),line:C.number().int().positive().describe("The line number (1-based, as shown in editors)"),character:C.number().int().positive().describe("The character offset (1-based, as shown in editors)"),query:C.string().optional()}),o=C.strictObject({operation:C.literal("workspaceSymbol"),filePath:C.string().describe("The absolute or relative path to the file"),line:C.number().int().positive().describe("The line number (1-based, as shown in editors)"),character:C.number().int().positive().describe("The character offset (1-based, as shown in editors)"),query:C.string().optional().describe("The symbol name or partial name to search for. Most language servers return no results for an empty query.")}),s=C.strictObject({operation:C.literal("goToImplementation"),filePath:C.string().describe("The absolute or relative path to the file"),line:C.number().int().positive().describe("The line number (1-based, as shown in editors)"),character:C.number().int().positive().describe("The character offset (1-based, as shown in editors)"),query:C.string().optional()}),i=C.strictObject({operation:C.literal("prepareCallHierarchy"),filePath:C.string().describe("The absolute or relative path to the file"),line:C.number().int().positive().describe("The line number (1-based, as shown in editors)"),character:C.number().int().positive().describe("The character offset (1-based, as shown in editors)"),query:C.string().optional()}),a=C.strictObject({operation:C.literal("incomingCalls"),filePath:C.string().describe("The absolute or relative path to the file"),line:C.number().int().positive().describe("The line number (1-based, as shown in editors)"),character:C.number().int().positive().describe("The character offset (1-based, as shown in editors)"),query:C.string().optional()}),l=C.strictObject({operation:C.literal("outgoingCalls"),filePath:C.string().describe("The absolute or relative path to the file"),line:C.number().int().positive().describe("The line number (1-based, as shown in editors)"),character:C.number().int().positive().describe("The character offset (1-based, as shown in editors)"),query:C.string().optional()});return C.discriminatedUnion("operation",[e,t,n,r,o,s,i,a,l])})});
export {XYa,QYa};
