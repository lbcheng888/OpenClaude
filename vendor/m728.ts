// @ts-nocheck
import {C} from "./m321.ts";
import {els,tls} from "./m727.ts";
import {b} from "../runtime.ts";
import {isBundledSkillsDisabled} from "./m726.ts";
import {Qr} from "./m323.ts";
import {ve} from "./m461.ts";
import {HOOK_EVENTS} from "./m723.ts";
function Uau(){let e=C.object({type:C.literal("command").describe("Shell command hook type"),command:C.string().describe("Shell command to execute"),args:C.array(C.string()).optional().describe("Argument list for exec form. When present, `command` is resolved as "+"an executable and spawned directly with these arguments \u2014 no shell. "+"Path placeholders like ${CLAUDE_PLUGIN_ROOT} are substituted per-element as plain strings, so paths with quotes, $, or backticks never reach a shell parser. When absent, `command` runs through a shell (bash on POSIX, PowerShell on Windows without Git Bash)."),if:VRt(),shell:C.enum(els).optional().describe("Shell interpreter. 'bash' uses your $SHELL (bash/zsh/sh); 'powershell' uses pwsh. Defaults to bash (powershell on Windows without Git Bash)."),timeout:C.number().positive().optional().describe("Timeout in seconds for this specific command"),statusMessage:C.string().optional().describe("Custom status message to display in spinner while hook runs"),once:C.boolean().optional().describe("If true, hook runs once and is removed after execution"),async:C.boolean().optional().describe("If true, hook runs in background without blocking"),asyncRewake:C.boolean().optional().describe("If true, hook runs in background and wakes the model on exit code 2 (blocking error). Implies async."),rewakeMessage:C.string().min(1).optional().describe("@internal Custom prefix for the system-reminder shown to the model when an asyncRewake hook exits with code 2. The hook output is appended after this prefix."),rewakeSummary:C.string().min(1).optional().describe('@internal One-line summary shown to the user in the terminal when an asyncRewake hook exits with code 2. Defaults to "Stop hook feedback".')}),t=C.object({type:C.literal("prompt").describe("LLM prompt hook type"),prompt:C.string().describe("Prompt to evaluate with LLM. Use $ARGUMENTS placeholder for hook input JSON."),if:VRt(),timeout:C.number().positive().optional().describe("Timeout in seconds for this specific prompt evaluation"),model:C.string().optional().describe('Model to use for this prompt hook (e.g., "claude-sonnet-4-6"). If not specified, uses the default small fast model.'),continueOnBlock:C.boolean().optional().describe(`Sets the continue value for the decision:"block" produced when ok is false. Default false (turn ends). Whether continue:true lets the turn proceed depends on the event's decision:"block" semantics. On PostToolUse, the reason is fed back to Claude and the turn continues.`),statusMessage:C.string().optional().describe("Custom status message to display in spinner while hook runs"),once:C.boolean().optional().describe("If true, hook runs once and is removed after execution")}),n=C.object({type:C.literal("mcp_tool").describe("MCP tool hook type"),server:C.string().describe("Name of an already-configured MCP server to invoke"),tool:C.string().describe("Name of the tool on that server to call"),input:C.record(C.string(),C.unknown()).optional().describe('Arguments passed to the MCP tool. String values support ${path} interpolation from the hook input JSON (e.g. "${tool_input.file_path}").'),if:VRt(),timeout:C.number().positive().optional().describe("Timeout in seconds for this specific tool call"),statusMessage:C.string().optional().describe("Custom status message to display in spinner while hook runs"),once:C.boolean().optional().describe("If true, hook runs once and is removed after execution")}),r=C.object({type:C.literal("http").describe("HTTP hook type"),url:C.string().url().describe("URL to POST the hook input JSON to"),if:VRt(),timeout:C.number().positive().optional().describe("Timeout in seconds for this specific request"),headers:C.record(C.string(),C.string()).optional().describe('Additional headers to include in the request. Values may reference environment variables using $VAR_NAME or ${VAR_NAME} syntax (e.g., "Authorization": "Bearer $MY_TOKEN"). Only variables listed in allowedEnvVars will be interpolated.'),allowedEnvVars:C.array(C.string()).optional().describe("Explicit list of environment variable names that may be interpolated in header values. Only variables listed here will be resolved; all other $VAR references are left as empty strings. Required for env var interpolation to work."),statusMessage:C.string().optional().describe("Custom status message to display in spinner while hook runs"),once:C.boolean().optional().describe("If true, hook runs once and is removed after execution")}),o=C.object({type:C.literal("agent").describe("Agentic verifier hook type"),prompt:C.string().describe('Prompt describing what to verify (e.g. "Verify that unit tests ran and passed."). Use $ARGUMENTS placeholder for hook input JSON.'),if:VRt(),timeout:C.number().positive().optional().describe("Timeout in seconds for agent execution (default 60)"),model:C.string().optional().describe('Model to use for this agent hook (e.g., "claude-sonnet-4-6"). If not specified, uses Haiku.'),statusMessage:C.string().optional().describe("Custom status message to display in spinner while hook runs"),once:C.boolean().optional().describe("If true, hook runs once and is removed after execution")});return{BashCommandHookSchema:e,PromptHookSchema:t,HttpHookSchema:r,AgentHookSchema:o,McpToolHookSchema:n}}
var VRt,nls,rls,p3;
var zon=b(()=>{isBundledSkillsDisabled();Qr();tls();VRt=ve(()=>C.string().optional().describe('Permission rule syntax to filter when this hook runs (e.g., "Bash(git *)"). Only runs if the tool call matches the pattern. Avoids spawning hooks for non-matching commands.'));nls=ve(()=>{let{BashCommandHookSchema:e,PromptHookSchema:t,AgentHookSchema:n,HttpHookSchema:r,McpToolHookSchema:o}=Uau();return C.discriminatedUnion("type",[e,t,n,r,o])}),rls=ve(()=>C.object({matcher:C.string().optional().describe('String pattern to match (e.g. tool names like "Write")'),hooks:C.array(nls()).describe("List of hooks to execute when the matcher matches")})),p3=ve(()=>C.partialRecord(C.enum(HOOK_EVENTS),C.array(rls())))});
export {Uau,VRt,nls,rls,p3,zon};
