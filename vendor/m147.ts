// @ts-nocheck
import {JJt,WorkloadIdentityError,Y6o,jJt,OAUTH_API_BETA_HEADER,FEDERATION_BETA_HEADER,W9,XJt,TX} from "./m140.ts";
import {initSessionMetadataPersistence} from "../src/core/0140_key.ts";
import {SX} from "./m141.ts";
import {b} from "../runtime.ts";
function u5o(e){return async()=>{JJt(e.baseURL);let t=await e.identityTokenProvider();if(t.length>16384)throw new WorkloadIdentityError(`Identity token is ${Math.ceil(t.length/1024)} KiB, exceeds the 16 KiB assertion limit`);let n={grant_type:Y6o,assertion:t,federation_rule_id:e.federationRuleId,organization_id:e.organizationId};if(e.serviceAccountId)n.service_account_id=e.serviceAccountId;if(e.workspaceId)n.workspace_id=e.workspaceId;let r=`${e.baseURL}${jJt}`,o;try{o=await e.fetch(r,{method:"POST",headers:{"Content-Type":"application/json","anthropic-beta":`${OAUTH_API_BETA_HEADER},${FEDERATION_BETA_HEADER}`,"User-Agent":e.userAgent||`anthropic-sdk-typescript/${initSessionMetadataPersistence} oidcFederationProvider`},body:JSON.stringify(n)})}catch(l){throw new WorkloadIdentityError(`Failed to reach token endpoint ${r}: ${l}`)}let s=o.headers.get("Request-Id");if(!o.ok){let l=await o.text().catch(()=>""),c=W9(l),u="";if(o.status===401)u=` Ensure your federation rule matches your identity token. ${e.workspaceId?"":"If your federation rule is scoped to multiple workspaces, set the ANTHROPIC_WORKSPACE_ID environment variable, the 'workspace_id' config key, or the `workspaceId` option. "}View your authentication events in the Workload identity page of Claude Console for more details.`;throw new WorkloadIdentityError(`Token exchange failed with status ${o.status}${s?` (request-id ${s})`:""}: ${c}${u}`,o.status,c,s)}let i=await XJt(o,s),a=Number(i.expires_in);if(!Number.isFinite(a))throw new WorkloadIdentityError(`Token endpoint response missing required fields: ${JSON.stringify(W9(i))}`,o.status,W9(i),s);return{token:i.access_token,expiresAt:SX()+a}}}
var d5o=b(()=>{TX()});
export {u5o,d5o};
