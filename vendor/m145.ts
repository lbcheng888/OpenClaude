// @ts-nocheck
import {gzt,WorkloadIdentityError,o$o,Azt,OAUTH_API_BETA_HEADER,FEDERATION_BETA_HEADER,C3,_zt,SX} from "./m138.ts";
import {MV} from "../src/core/0138_key.ts";
import {bX} from "./m139.ts";
import {b} from "../runtime.ts";
function y$o(e){return async()=>{gzt(e.baseURL);let t=await e.identityTokenProvider();if(t.length>16384)throw new WorkloadIdentityError(`Identity token is ${Math.ceil(t.length/1024)} KiB, exceeds the 16 KiB assertion limit`);let n={grant_type:o$o,assertion:t,federation_rule_id:e.federationRuleId,organization_id:e.organizationId};if(e.serviceAccountId)n.service_account_id=e.serviceAccountId;if(e.workspaceId)n.workspace_id=e.workspaceId;let r=`${e.baseURL}${Azt}`,o;try{o=await e.fetch(r,{method:"POST",headers:{"Content-Type":"application/json","anthropic-beta":`${OAUTH_API_BETA_HEADER},${FEDERATION_BETA_HEADER}`,"User-Agent":e.userAgent||`anthropic-sdk-typescript/${MV} oidcFederationProvider`},body:JSON.stringify(n)})}catch(l){throw new WorkloadIdentityError(`Failed to reach token endpoint ${r}: ${l}`)}let s=o.headers.get("Request-Id");if(!o.ok){let l=await o.text().catch(()=>""),c=C3(l),u="";if(o.status===401)u=` Ensure your federation rule matches your identity token. ${e.workspaceId?"":"If your federation rule is scoped to multiple workspaces, set the ANTHROPIC_WORKSPACE_ID environment variable, the 'workspace_id' config key, or the `workspaceId` option. "}View your authentication events in the Workload identity page of Claude Console for more details.`;throw new WorkloadIdentityError(`Token exchange failed with status ${o.status}${s?` (request-id ${s})`:""}: ${c}${u}`,o.status,c,s)}let i=await _zt(o,s),a=Number(i.expires_in);if(!Number.isFinite(a))throw new WorkloadIdentityError(`Token endpoint response missing required fields: ${JSON.stringify(C3(i))}`,o.status,C3(i),s);return{token:i.access_token,expiresAt:bX()+a}}}
var T$o=b(()=>{SX()});
export {y$o,T$o};
